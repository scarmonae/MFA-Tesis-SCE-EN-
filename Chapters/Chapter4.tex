\chapter{Optical Characterization}
\label{Chapter4} 

\noindent This chapter details the materials and methods used for the development of this work. Initially, the materials employed are described, including specific equipment, light sources, calibration patterns, and software used for data acquisition, processing, and analysis. Subsequently, the experimental methods are presented, organized according to the type of measurement performed: geometric and spectral measurements. For each method, the experimental procedure is clearly described, justifying the choice of technique and specifying the conditions under which the experiments were conducted, including details of instrument configuration and protocols followed. In addition, acknowledgments are due to René Restrepo Gómez, coordinator of the Optics Group, for his advice on the design and characterization of the optical systems; Herica Yulied Montoya, head of the Physics Laboratories, for granting access to the teaching facilities where all optical experiments were conducted; Cristian Lopera Trujillo, fellow student in the Master's in Applied Physics, for his assistance during the laboratory setups and experiments; and Raúl Andrés Castañeda Quintero, Associate Professor at Universidad EAFIT, for his guidance and advice in the design of the optical experiments.\\


% Finalmente, se incluye una breve descripción de los resultados esperados tras la ejecución de los métodos mencionados, acompañada del cronograma previsto para las actividades y el presupuesto estimado requerido para la implementación del estudio. Esta información busca proporcionar claridad y facilitar la replicabilidad del estudio, así como sustentar la factibilidad y pertinencia metodológica del proyecto.


% \section{Parámetros Espectrales para la Determinación de Índices de Vegetación}

% Para una caracterización efectiva y la calibración de cámaras multiespectrales destinadas a la medición de índices de vegetación, es crucial considerar los siguientes parámetros espectrales:

% \subsection{Sensibilidad Espectral del Sensor}
% \begin{itemize}
%     \item \textbf{Rango Espectral}: Define las longitudes de onda a las que el sensor es sensible. Para la mayoría de las aplicaciones en vegetación, es esencial cubrir desde el espectro visible (400-700 nm) hasta el infrarrojo cercano (700-1100 nm). Esta sensibilidad permite la detección precisa de la reflectancia de la vegetación, lo cual es crítico para índices como el NDVI \cite{Huang2021ASensing}.
%     \item \textbf{Sensibilidad Relativa}: Mide la respuesta del sensor a diferentes longitudes de on  da dentro del rango espectral. La sensibilidad relativa determina qué tan bien el sensor puede diferenciar entre diversas características de la vegetación basándose en sus firmas espectrales \cite{Dale2013HyperspectralReview, Klein2011MethodsCameras}.
% \end{itemize}

% \subsection{Transmisión Espectral del Objetivo (Lente)}
% \begin{itemize}
%     \item \textbf{Coeficiente de Transmisión Espectral}: Este parámetro describe la proporción de luz transmitida a través del objetivo en función de la longitud de onda. Es fundamental evaluar cómo los objetivos afectan la intensidad y calidad de la luz que llega al sensor para evitar distorsiones espectrales que puedan afectar la precisión de los índices de vegetación \cite{Xue2017SignificantApplications}.
%     \item \textbf{Rango de Transmisión Espectral}: El objetivo debe permitir la transmisión efectiva de las longitudes de onda relevantes (por ejemplo, bandas roja e infrarroja cercana) utilizadas para el cálculo de índices de vegetación. El rango de transmisión afecta directamente la calidad de la señal recibida por el sensor \cite{Dale2013HyperspectralReview}.
% \end{itemize}

% \subsection{Características Espectrales del Filtro}
% \begin{itemize}
%     \item \textbf{Curva de Transmitancia Espectral}: Define cómo el filtro permite o bloquea diferentes longitudes de onda. Es esencial para asegurar que sólo las longitudes de onda deseadas alcancen el sensor, eliminando posibles interferencias de otras bandas espectrales \cite{Huang2021ASensing}.
%     \item \textbf{Longitud de Onda de Corte y Ancho de Banda de Transmisión}: La longitud de onda de corte indica el punto donde el filtro comienza a bloquear la luz, mientras que el ancho de banda de transmisión especifica el rango de longitudes de onda que el filtro permite pasar. Estos parámetros son clave para seleccionar y diferenciar bandas espectrales específicas necesarias para índices de vegetación \cite{Dale2013HyperspectralReview}.
% \end{itemize}

% \subsection{Respuesta Espectral Combinada del Sistema}
% \begin{itemize}
%     \item \textbf{Eficiencia Espectral Total}: Considera la eficiencia combinada de sensor, objetivo y filtro para captar la luz en las longitudes de onda relevantes. Esto incluye la integración de todas las respuestas espectrales de los componentes individuales para determinar la capacidad del sistema completo en la detección y medición de índices de vegetación \cite{Xue2017SignificantApplications}.
%     \item \textbf{Linealidad y Estabilidad Espectral}: Evalúa cómo se mantiene la respuesta espectral del sistema bajo diferentes condiciones de iluminación y ambientales. La estabilidad espectral es crítica para aplicaciones en campo donde las condiciones pueden variar significativamente \cite{Dale2013HyperspectralReview}.
% \end{itemize}

% Estos parámetros deben ser cuidadosamente caracterizados y calibrados para asegurar que la cámara multiespectral proporcione datos precisos y fiables para la medición de índices de vegetación, facilitando así aplicaciones como el monitoreo de la salud de los cultivos, la evaluación del estrés vegetal y el análisis de cambios en la cobertura vegetal.


\section{Geometric Characterization}




\subsection{Determination of Field of View (FOV) and Angular Field of View (AFOV)}
\label{sec:fov}

\noindent In optical systems, transverse magnification provides the fundamental link between an object's physical dimensions and the size of its projection onto an image sensor. Also known as lateral or linear magnification ($m$), it is formally defined as the ratio of the image height ($h_i$) to the object height ($h_o$), with both measured perpendicularly to the optical axis:

\begin{equation}
    m = \frac{h_i}{h_o}
\end{equation}

\noindent This dimensionless factor indicates how much larger or smaller the image is compared to the object. If $|m| > 1$, the image is magnified; if $|m| < 1$, the image is reduced. A negative sign in $m$ indicates that the image is inverted with respect to the object, which is common in many imaging systems.

\noindent The transverse magnification can also be expressed in terms of the distances from the lens to the image ($d_i$) and from the lens to the object ($d_o$):

\begin{equation}
    m = -\frac{d_i}{d_o}
\end{equation}

\noindent This relationship, derived from the thin lens equation, is crucial for designing optical systems, as it directly links the position and size of the object with the position and size of the image formed on the sensor. Understanding magnification is, therefore, the first step in determining the system's field of view (FOV), as the FOV depends on the size of the sensor (image space) and how it projects onto the object space through the system's optics.\\


\noindent Before analysing the experimental data it is useful to establish the theoretical dependence of FOV on distance. In an ideal image-forming optical system, the linear relationship between the field of view (FOV) and the working distance arises from basic geometric optics. Under the pinhole camera model, the scene geometry is projected through a central projection point (the pinhole), forming a similar triangle between the object field and the sensor plane \cite{Hecht2002Optics,StanfordPinhole}. If the sensor has width $H$ and the lens has focal length $f$, the angular field of view (AFOV) is defined as:

\begin{equation}
\text{AFOV} = 2 \cdot \arctan\left(\frac{H}{2f}\right).
\end{equation}

\noindent
At a working distance $d$, the linear field of view on the object side is then given by:

\begin{equation}
\text{FOV}(d) = 2 \cdot d \cdot \tan\left(\frac{\text{AFOV}}{2}\right),
\label{eq:fov_linear}
\end{equation}

\noindent
This expression implies a direct proportionality between FOV and $d$, since $\tan(\text{AFOV}/2)$ is constant for a fixed sensor and focal length. For small angles ($\leq 30^\circ$), the tangent can be approximated as:

\begin{equation}
\tan\left(\frac{\text{AFOV}}{2}\right) \approx \frac{H}{2f},
\end{equation}

\noindent
yielding the simplified linear relationship:

\begin{equation}
\text{FOV}(d) \approx \frac{H}{f} \cdot d.
\end{equation}

\noindent
Thus, the rate of change of the FOV with respect to the working distance is constant and given by:

\begin{equation}
\frac{d\,\text{FOV}}{d\,d} = \frac{H}{f},
\end{equation}

\noindent
which is the linear magnification per unit distance. This linear approximation remains valid for small to moderate AFOV values, and forms the theoretical basis for modeling the FOV as a linear function of working distance in this study \cite{Hecht2002Optics}. Crucially, this linear rate of change enables laboratory validation at manageable working distances while informing the design of optical systems intended to operate at kilometer-scale ranges, streamlining both design and validation.\\


\noindent For this work, the Field of View (FOV) of the optical system is determined in order to assess the maximum ground area that can be observed by the system, depending on the distance between the optical system and the surface. For this, an optical setup is conducted in a laboratory to measure the FOV as the distance varies. The optical setup is shown in Figure~\ref{fig:MTF_FOV_montage}. First, a test chart is placed on a stable base located on an optical bench, preferably using a clamp that allows proper orientation with respect to the optical axis of the system. In this case, the test chart used is an ISO-12233 spatial frequency response test chart (see Figure \ref{fig:iso12233_test_chart}) that comes with multiple sensor format sizes, including an option for a 4:3 sensor aspect ratio. The reason for selecting this pattern is that it contains multiple elements whose length and distance between them can be measured, which allows and facilitates varying the working distance between the optical system being characterized and the chart, such that the edges of the captured image align with a pair of elements in the pattern. It is essential that the chart is properly positioned, centered, and perpendicular to the system's optical axis to minimize errors caused by tilts or misalignments. Next, the optical system is positioned on its corresponding base on the optical bench, ensuring it is centered and properly aligned with the test film.\\ 

\noindent Once the initial setup is established, images of the film are captured at different working distances. During image acquisition, an effort is made to precisely align the visible edges of the image with a known edge on the test film. Measurements are taken strictly in the vertical or horizontal direction, explicitly measuring the distance between objects located at opposite edges of the image. This procedure is carried out for both the horizontal and vertical FOV, as they may differ depending on the format of the optical sensor. In this case, The sensor used in this setup captures images with a 4:3 aspect ratio, which is typical for many scientific and industrial cameras. 


\begin{figure}[H]
        \centering
        \includegraphics[width=0.98\linewidth]{Figures/C3/MTF_montaje.pdf}
        \caption{Optical setup developed. The test chart used is an ISO-12233 spatial frequency response test chart printed in mate paper with a size of 69 $\times$ 42 cm. In this case, the use of barriers to prevent capture of unwanted light directly from the illumination source was not necessary, as the source is located behind the optical system under test. The illumination source is a tungsten halogen lamp coupled with a fiber optic cable and a diffuser.}
        \label{fig:MTF_FOV_montage}
\end{figure}


\noindent Since FOV measurements have inherent uncertainties due to the measuring instrument and other experimental sources, a statistical analysis using weighted linear regression is implemented to properly model the functional relationship between the measured FOV and the working distance. This method explicitly considers errors in both the dependent and independent variables, allowing for consistent and less biased estimates of the model parameters. This statistical analysis is needed in order to extrapolate the FOV at a working distances much greater than the used in the measurements, to account for errors in the model and how these propagate along the working distance. The weighted linear regression is fitted using total least squares (Deming regression), which simultaneously minimizes deviations in both variables:

\begin{equation}
\text{FOV} = \beta_1 \cdot \text{Working distance} + \beta_0,
\label{eq:regresion_lineal}
\end{equation}

\noindent where $\beta_1$ is the slope and $\beta_0$ is the intercept, both calculated along with their associated uncertainties, thus ensuring a statistically reliable model to describe the behavior of the FOV as a function of working distance.\\

\noindent Unlike ordinary least squares (OLS) regression, which assumes uncertainty only in the dependent variable, Deming regression ---also known as total least squares regression--- accounts for errors in both variables by minimizing the orthogonal deviations from the data points to the fitted line \cite{Deming1943ErrorsInVariables}. In this study, this method was adopted since both the working distance and the field of view (FOV) were measured experimentally using instruments with finite resolution (a tape measure and a millimeter ruler), which introduce measurement uncertainties of approximately \SI{1}{\milli\meter} in both variables. \\

\noindent Deming regression requires specifying a variance ratio $\delta = \sigma_y^2 / \sigma_x^2$ between the errors in the two variables. In this case, since both uncertainties are comparable, we adopt $\delta=1$. Under this assumption, the coefficients of the fitted linear model $y = \beta_1 x + \beta_0$ are calculated as:

\begin{equation}
\hat{\beta}_1 = \frac{s_{yy} - s_{xx} + \sqrt{(s_{yy} - s_{xx})^2 + 4s_{xy}^2}}{2s_{xy}},
\end{equation}

\noindent where $s_{xx}$ and $s_{yy}$ are the sample variances of $x$ (working distance) and $y$ (FOV), and $s_{xy}$ is their covariance. The intercept is then computed as:

\begin{equation}
\hat{\beta}_0 = \bar{y} - \hat{\beta}_1\,\bar{x},
\end{equation}

\noindent ensuring that the fitted line passes through the centroid of the data.\\

\noindent To minimize errors introduced by optical aberrations, such as spherical aberration, the measurements are preferably taken along the central horizontal and vertical axis of the image.\\

\noindent Regarding the system's focusing, it is recommended to use an autofocus mechanism for proper image acquisition of the test chart. Since the optical systems used in this study have a fixed focal length, the focus must be manually adjusted to obtain a high-quality image for each working distance of each observation. To optimize this adjustment, a method is used based on capturing an image of a well-defined edge, from which the corresponding intensity profile is extracted and displayed in real-time, in this case along the central horizontal line. The derivative of this profile is then calculated and plotted to represent the intensity variation across the edge transition. Focus optimization is achieved by maximizing the prominence of the peak in the derivative, indicating a higher definition in the signal variation. This point of maximum variation is considered indicative of correct focus. Figure~\ref{fig:enfoque_conjunta} shows a representative example of optimal edge focusing.\\

\noindent Figure~\ref{fig:enfoque_conjunta} illustrates the image set used for manual focusing. The first image shows the captured edge, with a low-intensity region on the left and a high-intensity region on the right; the sensor is set to zero gain and an exposure time that avoids saturation while maximizing the dynamic range between dark and bright areas. The second image presents the central horizontal profile, where the levels in both regions and their transition are observed. The third image corresponds to the profile derivative, which remains near zero in the dark and bright regions but exhibits a peak at the transition; the height of that peak is used as the focus metric.\\

\begin{figure}[H]
    \centering
    % First subfigure
    \begin{subfigure}[b]{0.7\linewidth}
        \centering
        \includegraphics[trim=0mm 0mm 0mm 10mm, clip, width=\linewidth]{Figures/C3/SE_1.png}
        \caption{Edge image used for manual focusing.}
        \label{fig:enfoque}
    \end{subfigure}
    
    \vspace{1em} % Vertical space between subfigures

    % Second subfigure
    \begin{subfigure}[b]{0.95\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/C3/SE_signal.png}
        \caption{Intensity profile extracted along the central horizontal line of the edge image.}
        \label{fig:se_signal}
    \end{subfigure}
    
    \vspace{1em} % Vertical space between subfigures

    % Third subfigure
    \begin{subfigure}[b]{0.95\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/C3/SE_diff.png}
        \caption{Derivative of the intensity profile; the maximum peak height is used as the focus metric.}
        \label{fig:se_diff}
    \end{subfigure}
    
    \caption{Set of images used for focus optimisation: (a) raw edge image, (b) corresponding intensity profile, and (c) derivative of that profile. The optimum focus position is identified when the derivative peak in (c) reaches its maximum amplitude, indicating the sharpest edge transition.}
    \label{fig:enfoque_conjunta}
\end{figure}


\subsection{Modulation Transfer Function (MTF) Characterization}
\label{sec:mtf_char}

Image formation in optical systems can be mathematically described as a convolution operation between the spatial irradiance distribution of the object and the impulse response of the optical system. This operation captures how an object is transformed into the final image generated by the system. Formally, the image $g(x,y)$ is obtained by:

\begin{equation} g(x,y) = f(x,y) * h(x,y), \end{equation}

\noindent where $f(x,y)$ is the spatial irradiance of the ideal object and $h(x,y)$ is the impulse response of the optical system, which characterizes the combined effects of physical phenomena such as diffraction and aberrations on the final image.\\

\noindent In an ideal optical system, the impulse response is a delta function ($\delta(x,y)$), resulting in an exact replica of the object. However, in real systems, the impulse response has a finite spatial distribution due to diffraction and aberration effects, causing even point sources to appear as blurred spots in the image, known as blur spots \cite{Boreman2001ModulationTT52}. This actual distribution, known as the Point Spread Function (PSF), defines the smallest detail that can be resolved by the optical system.\\

\noindent The convolution process requires the system to meet the properties of linearity and spatial invariance (LSI), implying that the functional form of h(x,y) does not change with position in the image plane. Although these conditions are only approximated in real systems due to field-angle-dependent aberrations, the convolutional analysis remains valid within isoplanatic regions, where the PSF stays approximately constant \cite{Boreman2001ModulationTT52}. This implies that a robust characterization of the system's performance requires measuring the PSF in multiple image regions (e.g., center, edges, and corners), rather than relying on a single on-axis measurement.\\

\noindent The Dirac delta function is a distribution that is zero everywhere except at the origin, where it is infinitely large, yet its integral over its entire domain is equal to one. The sifting property states that when a function is multiplied by a shifted Dirac delta and integrated, the result is the value of the function at the point of the shift. This is formally expressed as:

\begin{equation}
f(x',y') = \iint f(x_{\text{obj}},y_{\text{obj}}) \delta(x'-x_{\text{obj}}, y'-y_{\text{obj}})\,dx_{\text{obj}}dy_{\text{obj}}.
\end{equation}

\noindent In this equation, $f(x_{\text{obj}}, y_{\text{obj}})$ represents the continuous radiance distribution of the scene in the object plane. The integral, operating over the object coordinates, effectively "samples" the continuous function $f(x_{\text{obj}}, y_{\text{obj}})$ at the specific location $(x', y')$ defined by the Dirac delta function. The result, $f(x',y')$, is the value of the scene's radiance at that precise point in the image plane. This equation forms the theoretical basis for understanding how a continuous optical image is converted into a set of discrete pixel values by an image sensor.\\

\noindent The convolution with the impulse response $h(x',y')$ of the optical system can be interpreted as follows: each point of the object $(x',y')$ acts as a point source whose contribution in the image plane is a shifted copy of the function $h(x,y)$, centered at the corresponding location. The intensity of this copy is weighted by the object irradiance at that point $f(x',y')$. The final image is then obtained by integrating all these individual contributions \cite{Boreman2001ModulationTT52}. Mathematically, this process is expressed as the convolution:

\begin{equation}
g(x,y) = \iint h(x-x',\,y-y')\, f(x',y') \, dx'\,dy',
\end{equation}

where $g(x,y)$ is the irradiance distribution in the image plane, $f(x',y')$ is the irradiance distribution of the object, and $h(x,y)$ is the point spread function (PSF) of the system.


\begin{figure}[H] 
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/image_formation.png}
    \caption{The convolution of the object with the impulse response to generate the image. Taken from \cite{Boreman2001ModulationTT52}.}
    \label{fig:enter-label}
\end{figure}

\noindent The Modulation Transfer Function (MTF) enables an objective evaluation of the optical system’s ability to reproduce detail and contrast at different spatial frequencies. Contrast is a key parameter in evaluating image quality, as it directly influences an optical system’s ability to reproduce details faithfully. Its impact on visual perception and optical system design is significant, especially in applications where resolution and sharpness are essential. Generally, contrast determines the differentiation between regions of different luminance in an image, affecting the perception of fine details and the visual quality in optical and electro-optical systems.\\

\noindent Intuitively, the MTF tells how much contrast survives at each level of detail: high MTF values at high spatial frequencies mean the system preserves fine structure, while low values indicate it blurs those details. The highest spatial frequency where the MTF remains meaningfully above zero sets the practical resolution limit; translating that cut-off to object space yields the smallest feature that can be distinguished (e.g., the ground-resolved distance in remote-sensing applications).\\

\noindent Contrast is generally expressed in terms of \textit{modulation depth}, defined as:

\begin{equation}
M = \frac{A_{\max} - A_{\min}}{A_{\max} + A_{\min}}
\label{eq:contraste}
\end{equation}

where:
\begin{itemize}
    \item $A_{\max}$ is the maximum intensity in the image.
    \item $A_{\min}$ is the minimum intensity in the image.
\end{itemize}

When $A_{\min} = 0$, the contrast is maximum $(M = 1)$. Conversely, if $A_{\max} = A_{\min}$, the modulation is null $(M = 0)$, implying that the image lacks intensity variations and, therefore, visual detail.\\

\noindent Contrast in an optical system is directly related to the \textit{Modulation Transfer Function} (MTF), which measures how modulation is preserved in the image across different spatial frequencies. The MTF is defined as:

\begin{equation}
\text{MTF}(f) = \frac{M_{\text{image}}(f)}{M_{\text{object}}(f)}
\label{mtf}
\end{equation}

where:
\begin{itemize}
    \item $M_{\text{image}}(f)$ is the measured modulation in the image for a spatial frequency $f$.
    \item $M_{\text{object}}(f)$ is the modulation of the object at the same frequency.
\end{itemize}

\noindent To analyze the system's performance in terms of spatial frequency, we use the Optical Transfer Function (OTF). The OTF is a crucial tool in imaging science as it provides a comprehensive description of how an optical system transfers the spatial frequency content of an object to the image. It is defined as the Fourier transform of the Point Spread Function (PSF):

\begin{equation}
\text{OTF}(u,v) = \mathcal{F} \{ \text{PSF}(x,y) \}.
\end{equation}


\noindent The Line Spread Function (LSF) is a function derived from the PSF and describes the response of the optical system when the excitation is a thin line instead of a point. It is obtained by integrating the PSF along one of its dimensions:

\begin{equation}
\text{LSF}(x) = \int_{-\infty}^{\infty} \text{PSF}(x,y) \, dy.
\end{equation}

\noindent The \textit{Edge Spread Function} (ESF) models the system's response to an abrupt edge transition. This function is particularly useful in the experimental measurement of the MTF, as it can be easily obtained from images of high-contrast edges. The ESF is related to the LSF through the derivative:

\begin{equation}
\text{LSF}(x) = \frac{d}{dx} \text{ESF}(x).
\end{equation}

\noindent The \textit{Modulation Transfer Function} (MTF) characterizes the ability of an optical system to transfer contrast of different spatial frequencies from the object to the image. It is obtained from the Fourier transform of the LSF:

\begin{equation}
\text{MTF}(u) = \left| \mathcal{F} \{ \text{LSF}(x) \} \right|.
\end{equation}

\noindent The OTF is a complex-valued function that operates in the frequency domain, where $(u,v)$ represent the spatial frequencies in the x and y directions, respectively. It fully characterizes the linear, space-invariant behavior of the optical system. The OTF can be separated into its magnitude and phase components:

\begin{equation} \text{OTF}(u,v) = \text{MTF}(u,v) \cdot e^{i\phi(u,v)}, \end{equation}

where: Modulation Transfer Function (MTF), $|\text{OTF}(u,v)|$, is the magnitude of the OTF. It describes the attenuation of contrast for each spatial frequency as it passes through the optical system. An MTF of 1 indicates perfect contrast transfer, while an MTF of 0 means the contrast at that frequency is completely lost. Phase Transfer Function (PTF), $\phi(u,v)$, is the phase of the OTF. It represents the phase shift (or spatial displacement) of each spatial frequency component. For an ideal, aberration-free optical system, the PSF is symmetric, and the PTF is zero for all frequencies. Non-zero PTF values are indicative of asymmetric aberrations in the optical system, which cause geometric distortions in the image.\\

\noindent In this work, the slanted-edge method is used, as established by the international standard ISO 12233. Because the MTF encodes the ultimate resolving capability of the lens–sensor pair, it naturally leads to the \emph{ground-resolved distance} (GRD), i.e. the smallest linear feature that can be distinguished on the ground at a given altitude.  Establishing a transparent link between laboratory MTF and mission-level GRD therefore allows direct verification of the resolution requirement.\\


\subsubsection{Experimental Setup}

\noindent The procedure begins by placing the ISO 12233 standard spatial resolution chart (see Figure~\ref{fig:iso12233_test_chart}) on a stable base, implementing the same setup as the mentioned in the Figure \ref{fig:MTF_FOV_montage} for the FOV characterization. Uniform illumination over the chart’s surface is ensured, guaranteeing that the light intensity at any point does not vary more than $\pm 10\%$ from the center value.\\

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{Figures/C3/ISO_12233-reschart.pdf}
    \caption{Standard chart used to measure the spatial frequency response of the optical system, as established in the ISO 12233 standard.}
    \label{fig:iso12233_test_chart}
\end{figure}

\noindent The illumination setup was based on the recommendations of the ISO 12233 standard, illustrated in Figure~\ref{fig:srf_motaje}. However, due to laboratory equipment limitations—specifically the absence of an additional light source or a fiber optic with a beam splitter—the lighting could only be positioned on one side of the optical axis, rather than on both sides as the standard suggests for achieving homogeneous illumination. Despite this necessary adaptation, measures were taken to make the illumination as uniform as possible. Ambient lighting was minimized, and a diffuser was employed to prevent unwanted intensity patterns from the light source.\\



\begin{figure}[H]
    \centering
    \includegraphics[trim=20mm 50mm 20mm 0mm, clip, width=0.7\linewidth]{Figures/C3/iso 12233 montaje.png}
    \caption{Optical setup recommended by ISO 12233:2024 for accurate MTF measurement. (1) matte black background, (2) test chart, (3) white illumination, (4) optical system under test, (5) barriers to prevent direct illumination of the optical system, (6) adjustable working distance.}
    \label{fig:srf_motaje}
\end{figure}

\noindent The illumination is arranged such that the signal distribution delivered by the optical system is as homogeneous as possible. This implies that the captured image should show two well-defined peaks: one for the dark areas and another for the bright areas of the test pattern. Ideally, these peaks would be delta functions, indicating sharp transitions between intensity regions. In practice, secondary peaks should be minimized and the main peaks should be as narrow as possible. A homogeneous distribution ensures the optical system’s response is measured under controlled conditions and the MTF analysis is not affected by unwanted illumination variations. Figure~\ref{fig:illumination_distribution} shows the distribution achieved for the region of interest shown in Figure~\ref{fig:enfoque}. Additionally, this distribution confirms that signal saturation is minimal in the region of interest.\\

\noindent The exposure time of the optical system are carefully adjusted to obtain a maximum signal without saturation in the bright areas of the target. Signal clipping must be avoided in both bright and dark regions, including the transition zones of the slanted edge. As image compression can significantly affect resolution measurements, all system settings must be recorded and reported, including focal length, aperture, sensor resolution, and compression modes used. When using color cameras, proper white balance must be applied according to the light source, as specified in ISO 14525. White balance may be performed automatically if the software and optical system support it, or manually using the RGB channel histogram. The goal is to overlap the histogram peaks of the three channels as much as possible. For this work, white balance was performed manually, ensuring that the peaks corresponding to the bright zone overlap across the three RGB channels. This signal distribution can be seen in Figure~\ref{fig:illumination_distribution}.\\


\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{Figures/C3/Histogram_MTF.pdf}
    \caption{Signal distribution delivered by the optical system under the configured experimental illumination. Two main peaks are observed: one in the dark zone and another in the bright zone, with attenuated secondary peaks. The horizontal axis represents the 8-bit pixel value (0--255) and the vertical axis shows the counts, as in a histogram.}
    \label{fig:illumination_distribution}
\end{figure}

\noindent Since the optical system under analysis does not have autofocus and instead requires manual adjustment, a Python script was implemented to display the camera or sensor image in real time, along with a window showing the central horizontal intensity profile and another window showing its derivative. To determine optimal focus, the method seeks to maximize the magnitude of the extreme values—peaks and valleys—of the derivative of the central profile, since a higher derivative corresponds to better edge resolution. This indicates that the focus point has been reached where high spatial frequency details are best reproduced. This method is the same as described in Section \ref{sec:fov}.\\


\noindent The optical system is positioned to properly frame the test chart. Vertical arrows help adjust magnification, while horizontal arrows assist in centering the image. The tips of the central black arrows should be fully visible, while the white arrows should be completely hidden. The horizontal edge of the chart should be aligned approximately parallel to the sensor’s horizontal lines. The approximate distance between the camera and the test chart is recorded for later analysis. Figure~\ref{fig:iso12233_test_chart_encuadre} shows the arrows used for alignment.\\

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Figures/C3/ISO_12233-reschart_arrows.png}
    \caption{Standard chart used to measure the spatial frequency response of the optical system. The arrows used for framing and camera alignment are highlighted.}
    \label{fig:iso12233_test_chart_encuadre}
\end{figure}


\noindent For each system (VIS and NIR) we captured 50 illuminated frames of the test and 50 dark frames (illumination turned off), keeping the working distance and exposure settings constant. Averaging the 50 frames mitigates random noise originating from shot-to-shot fluctuations in both the light source and the sensor, producing a single noise-reduced image.  
The dark frames, which contain only the sensor’s baseline signal (dark current) and fixed-pattern artefacts such as hot or dead pixels, are likewise averaged into a master dark frame.  
This master dark frame is then subtracted from every illuminated frame prior to further processing, as detailed in Section~\ref{subsub:corriente_oscura_espectrometro}. A specific analysis region is selected within the corrected image, where the slanted edge is clearly distinguishable.\\


\noindent MTF processing is performed using the \href{https://imagej.net/ij/plugins/se-mtf/index.html}{SE\_MTF} plugin from \href{https://imagej.net/ij/index.html}{ImageJ} software. In this procedure, the image scale is configured in physical units (e.g., lp/mm) based on the actual sensor size and number of photodetectors. Once parameters are configured, the plugin extracts the Edge Spread Function (ESF) from the image edge and, through numerical differentiation, generates the Line Spread Function (LSF). Then, a Fast Fourier Transform (FFT) is applied to the LSF to obtain the Modulation Transfer Function (MTF). Figure~\ref{fig:plugin_config1} shows the interface for configuring parameters. Based on the Region of Interest (ROI) and pixel size, it is possible to compute the required parameters for plugin configuration.\\

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{Figures/C3/imagej_conf.png}
    \caption{Configuration screen of the SE\_MTF plugin in ImageJ. Setup for a Region of Interest of 104 $\times$ 104 pixels and a pixel size of 2.2 $\mu \text{m}$.}
    \label{fig:plugin_config1}
\end{figure}

\subsubsection{Ground–Resolved distance (GRD) derived from the MTF}
\label{subsub:grd_from_mtf}

The Modulation Transfer Function provides a frequency–domain representation of the
optical system’s ability to reproduce scene detail.  To translate the laboratory MTF into a
practical “ground” resolution (often called the \emph{ground‑resolved distance}, GRD), the
following procedure is applied\footnote{The workflow follows the recommendations of
ISO~12233 and common practice in remote‑sensing optics
\cite{Boreman2001ModulationTT52, Holst2008ElectroOptical}.}.

\paragraph{1. Identify the cut-off spatial frequency in the image plane.}
From the measured MTF curve, the spatial frequency
$f_{\text{im}}$ (in line-pairs per millimetre, lp/mm) at which the modulation falls to
30\,\% is chosen as the \emph{cut-off} frequency:
%
\begin{equation}
    \text{MTF}\!\left(f_{\text{im}}\right)=0.30.
    \label{mtf_cutoff}
\end{equation}

\noindent
The 30\,\% level is a widely accepted engineering compromise.  
At this point an object retains roughly one-third of its original contrast after passing
through the optical chain, which is still sufficient for reliable visual or algorithmic
detection.  
Lower thresholds (e.g.\ 10\,\%) would push the measurement into a region where the
residual contrast approaches the image‐noise floor—dominated by shot noise,
read-out noise and illumination fluctuations—making it difficult to distinguish true
detail from artefacts.  
Conversely, higher thresholds (50\,\% or 70\,\%) would under-estimate the usable
resolution by ignoring spatial frequencies that remain perceptible above the noise.
Choosing 30\,\% therefore provides a practical margin: features with modulation
greater than this value stand clearly above the contrast variations introduced by the
sensor, exposure time and lighting instabilities, while frequencies beyond
$f_{\text{im}}$ are considered effectively lost in noise.

\paragraph{2. Convert to object‑space frequency.}
The primary lateral magnification of the lens is
%
\begin{equation}
    \label{eq:pmag}
    \text{PMAG}= \frac{\text{Horizontal Sensor Size}}{\text{FOV}}.
\end{equation}
%
The corresponding frequency in object space is therefore
%
\begin{equation}
    f_{\text{obj}} = f_{\text{im}} \times \text{PMAG}
    \quad\left[\frac{\text{lp}}{\text{mm}}\right].
    \label{eq:f_obj_4}
\end{equation}

\paragraph{3. Compute the linear ground-resolved distance.}
A line pair consists of one dark and one bright line; hence the minimum
distinguishable physical period on the ground \cite{HollowsJames_Resolution_EO}, expressed in micrometres, is
%
\begin{equation}
    \Delta_{\text{ground}} =
    \frac{1}{2\,f_{\text{obj}}}\;\times\;
    \frac{1000\;\mu\text{m}}{1\;\text{mm}}.
    \label{eq:delta_ground}
\end{equation}
%

\paragraph{4. Reporting.}
For every lens/sensor pair characterised in
Section~\ref{sec:fov}--\ref{subsub:grd_from_mtf},
Table~\ref{tab:grd_results} lists
$f_{\text{im}}$ at 30\,\% contrast, the computed $f_{\text{obj}}$, and the resulting
$\Delta_{\text{ground}}$ for the three mission altitudes analysed
(50\,m, 1000\,m, and 5000\,m).

\begin{table}[h]
    \centering
    \caption{Ground‑resolved distance (GRD) derived from the 30 \%‑contrast MTF cut‑off. 
             Placeholder numbers are shown; replace them with the values measured for
             each optical system.}
    \label{tab:grd_results}
    \begin{tabularx}{\linewidth}{|l|C|C|C|C|C|}
        \hline
        \rowcolor[HTML]{EFEFEF}
        \textbf{System} & 
        $f_{\text{im}}$ [lp/mm] & 
        $f_{\text{obj}}$ [lp/mm] & 
        GRD @50\,m [cm] & 
        GRD @1000\,m [m] &
        GRD @5000\,m [m] \\
        \hline
        VIS & - & - & - & - & - \\
        NIR & - & - & - & - & - \\
        \hline
    \end{tabularx}
\end{table}

%===================================================================


\subsection{Acquisition Settings for VIS and NIR Systems}

The accuracy of resolution and MTF measurements largely depends on the optical system configuration during image capture. In this study, all configurations used for each system were recorded and reported. The following tables summarize the parameters used for the VIS system (visible spectrum camera) and for the NIR system (near-infrared camera):

\begin{longtable}[c]{c|cc|cc}
\hline
\rowcolor[HTML]{EFEFEF} 
\cellcolor[HTML]{EFEFEF} &
  \multicolumn{2}{c|}{\cellcolor[HTML]{EFEFEF}\textbf{FOV}} &
  \multicolumn{2}{c}{\cellcolor[HTML]{EFEFEF}\textbf{MTF}} \\
\rowcolor[HTML]{EFEFEF} 
\multirow{-2}{*}{\cellcolor[HTML]{EFEFEF}\textbf{Parameter}} &
  \textbf{VIS} &
  \textbf{NIR} &
  \textbf{VIS} &
  \textbf{NIR} \\ \hline
\endfirsthead
%
\endhead
%
\hline
\endfoot
%
\endlastfoot
%
\textbf{ExposureTime (ms)}  & 18264.15 & 498463.3 & 2636.135 & 47487.15 \\
\rowcolor[HTML]{EFEFEF} 
\textbf{Gain}               & 0.0      & 24.1     & 0.0      & 0.0      \\
\textbf{Height (pixels)}    & 1944     & 1944     & 104      & 104      \\
\rowcolor[HTML]{EFEFEF} 
\textbf{Width (pixels)}     & 2592     & 2592     & 104      & 104      \\
\textbf{OffsetX}            & 0        & 0        & 1230     & 1200     \\
\rowcolor[HTML]{EFEFEF} 
\textbf{OffsetY}            & 0        & 0        & 920      & 900      \\
\textbf{Gamma}              & 1.0      & 1.0      & 1.0      & 1.0      \\
\rowcolor[HTML]{EFEFEF} 
\textbf{Hue}                & 0.0      & --       & 0.0      & --       \\
\textbf{BalanceRatio\_Red}  & 1.307617 & --       & 1.289063 & --       \\
\rowcolor[HTML]{EFEFEF} 
\textbf{BalanceRatio\_Blue} & 1.957031 & --       & 1.979004 & --       \\ \hline
\caption{Acquisition Configuration for MTF and FOV Experiments (VIS and NIR)}
\label{tab:tab:fov_mtf}\\
\end{longtable}

\noindent Below is a brief description of each parameter used in the configuration:

\begin{itemize}
    \item \textbf{ExposureTime:} Exposure time in milliseconds, determining how long the sensor collects light. Longer exposures increase the captured light but may cause saturation if not properly adjusted.

    \item \textbf{Gain:} Sensor gain factor, amplifying the generated electrical signal. A high gain may increase noise; hence, it is set to 0.0 to avoid unwanted amplification.

    \item \textbf{Gamma:} Gamma correction applied to the image, adjusting the linearity of the light response. A value of 1.0 ensures a linear response model.

    \item \textbf{Height (pixels):} Image height in pixels. This parameter defines the vertical resolution and the area of interest.

    \item \textbf{Width (pixels):} Image width in pixels. Together with height, it defines the spatial resolution of the sensor.

    \item \textbf{OffsetX and OffsetY:} Horizontal (OffsetX) and vertical (OffsetY) displacements of the capture area over the sensor. These values help center or adjust the region of interest according to the position of the object or test chart.

    \item \textbf{Hue:} Hue adjustment in the image, used only in color cameras (VIS system). A value of 0.0 means no tonal adjustment is applied.

    \item \textbf{BalanceRatio\_Red and BalanceRatio\_Blue:} White balance ratios for the red and blue channels in color cameras. These coefficients are used to match the responses of each channel and ensure proper white balance. In this study, they were manually adjusted to overlap the peaks of the bright zone in the RGB histograms.
\end{itemize}

\noindent These settings are defined to ensure maximum signal without saturation and to avoid clipping in bright and dark areas, guaranteeing that the slanted-edge image is captured under controlled conditions. These parameters are recorded to allow for study replication and for detailed analysis of the optical system response under both low- and high-intensity conditions.

\section{Spectral characterization of multispectral cameras}
\label{sec:spectral_characterization}

\noindent The radiometric performance of an imaging system is determined by its capacity to convert the spectral radiance originating from a target scene into a measurable energy flux at the detector plane. For an extended, spatially uniform source—such as a calibration panel or a large, homogeneous ground area—the incident radiation is quantified by its \textit{spectral radiance}, \(L(\lambda)\). This fundamental quantity represents the optical power per unit source area, per unit solid angle, and per unit wavelength.\\

\noindent The transformation of this scene radiance into energy per unit area, \(Q_{\mathrm d}\), at the image plane is described by the camera equation. For an optical system operating under narrowband conditions around a central wavelength \(\lambda\), the radiant exposure accumulated over a time \(\tau\) at a field angle \(\theta\) is given by \cite{joseph2015building-ebook}:

\begin{equation}
    Q_{\mathrm d}(\theta,\lambda)
    \;=\;
    \frac{\pi}{4\,(F/\#)^{2}}\; O_{\mathrm e}(\lambda)\;\Delta \lambda\,L(\lambda)\;\tau\;\cos^{4}\!\theta
    \qquad\big[\mathrm{J\,m^{-2}}\big].
    \label{eq:Qd_narrow_thesis}
\end{equation}

\noindent An analysis of the terms in Equation~\eqref{eq:Qd_narrow_thesis} reveals the principal factors governing the collected energy. The term \(Q_{\mathrm d}(\theta,\lambda)\) is the \textit{radiant energy} at the detector \cite{joseph2015building-ebook}, defined as the time integral of the image irradiance (\(Q_{\mathrm d}=E_{\mathrm d}\,\tau\)). The system's light-gathering ability is inversely proportional to the square of the F-number (\(F/\#\)), which is the ratio of the focal length to the aperture diameter. Consequently, optically faster systems (those with a lower \(F/\#\)) deliver more energy. The collected signal is also linearly proportional to the total Optical Efficiency (\(O_{\mathrm e}(\lambda)\)), which accounts for transmissive and reflective losses within the optical path; the spectral radiance of the scene (\(L(\lambda)\)); the effective spectral bandwidth (\(\Delta\lambda\)); and the exposure time (\(\tau\)).\\

\noindent The operational significance of this radiometric model is substantial. It establishes a quantitative link between the physical properties of the target scene and the expected signal at the sensor. Since detector responsivity is typically specified as a voltage or current output per unit of incident energy density (e.g., in V/\(\mu\mathrm{J}\,\mathrm{cm}^{-2}\)), Equation~\eqref{eq:Qd_narrow_thesis} facilitates the direct prediction of signal levels. This, in turn, allows for robust estimation of the signal-to-noise ratio (SNR), making the model an essential tool for system design, mission planning, and data analysis. Furthermore, for extended targets that completely fill the sensor's instantaneous field of view (IFOV), the radiant flux at the detector becomes effectively independent of the sensor's altitude, a principle that greatly simplifies radiometric modeling for applications at varying operational heights.\\

\noindent Spectral characterization of a multispectral imaging system consists in determining its relative spectral response, $S(\lambda)$, which encapsulates the combined effects of lenses, filter stack and detector in a single transfer function. Experimentally, $S(\lambda)$ is retrieved by measuring the digital output $C$ produced by the system when illuminated with a source of known spectral radiance $L(\lambda)$ and fitting the continuous model

\begin{equation}
    C \;\propto\;\int S(\lambda)\,L(\lambda)\,\mathrm{d}\lambda
\label{eq:spectral_transfer}
\end{equation}

\noindent using suitable regression techniques \cite{Uttner2006SpectralCameras}. By condensing all optical losses and spectral crosstalk into $S(\lambda)$, this function becomes essential for performing accurate radiometric corrections, ensuring precise inter-band registration, and enabling the quantitative retrieval of biophysical variables, all of which depend on sub-percent differences between spectral channels. In this thesis, the resulting $S(\lambda)$ curves are presented in arbitrary units (relative reflectance), since assigning an absolute scale would require a spectroradiometer and optical power meter calibrated against nationally traceable standards, which is beyond the scope of the present work.  


\subsection{Spectral Response of RGB Color Cameras}

\noindent
In digital RGB cameras, each channel (red, green, and blue) responds to incoming radiation according to its own \emph{spectral sensitivity} curve. Depending on the sensor design and the in-camera signal processing, this response can often be treated as linear, but manufacturers may also introduce static nonlinear mappings for tonal rendering and dynamic-range compression \cite{Vora1997DigitalModels,Cheung2004AccurateCameras,Uttner2006SpectralCameras}.\\


\noindent
Let \(K\) denote the number of channels (typically \(K=3\) for \(R\), \(G\), and \(B\)). For a given channel \(i\), the recorded signal \(r_i\) is modeled as the time-integrated projection of the incident spectrum onto the channel’s spectral sensitivity \(s_i(\lambda)\):

\begin{equation}
r_{i} \;=\; t_{\mathrm{integ}} \int_{\lambda_{\min}}^{\lambda_{\max}} s_{i}(\lambda)\,I(\lambda)\,\mathrm{d}\lambda \;+\; n_{i},
\label{eq:linear_response}
\end{equation}

\noindent
where \(I(\lambda)\) represents the spectral power incident on the sensor for the pixel under consideration (i.e., the illuminant shaped by the object's reflectance and the imaging geometry), \(t_{\mathrm{integ}}\) is the exposure (integration) time, and \(n_i\) aggregates additive terms such as dark offset and read noise. When the sensor chain is linear and no nonlinear firmware processing is applied, Equation~\eqref{eq:linear_response} captures the dominant behavior: the digital value grows proportionally with incident energy so long as the measurement remains below saturation and the noise is approximately stationary \cite{Vora1997DigitalModels}.\\

\noindent
Many cameras apply a \emph{gamma} or similar static nonlinearity to the internal linear signal for perceptual rendering or range compression. In that case the observable response is written as a monotone mapping \(F(\cdot)\) of the linear signal,
\begin{equation}
r_{i} \;=\; F\!\Bigl(\,t_{\mathrm{integ}}\!\!\int_{\lambda_{\min}}^{\lambda_{\max}} s_{i}(\lambda)\,I(\lambda)\,\mathrm{d}\lambda \;+\; n_{i}\Bigr).
\label{eq:nonlinear_response}
\end{equation}

\noindent
A practical approximation is a power law, \(r_i = \rho_i^{\,\gamma_i}\), where \(\rho_i\) denotes the internal linear signal and \(\gamma_i\) is the channel’s encoding exponent. Linearization then proceeds by applying the inverse, \(\rho_i = r_i^{\,1/\gamma_i}\). Recovering \(\rho_i\) is essential for radiometric tasks such as calibration, white balancing, and the estimation of spectral sensitivity, since these require proportionality between measured values and incident energy \cite{Cheung2004AccurateCameras,Uttner2006SpectralCameras}.\\

% \subsubsection{Spectral Sensitivity Characterization}

% \noindent
% The spectral sensitivity \(s_i(\lambda)\) quantifies the efficiency with which channel \(i\) detects radiation at wavelength \(\lambda\). Direct determination with a monochromator is the most accurate approach but is often impractical in laboratory settings. Indirect methods therefore estimate \(s_i(\lambda)\) using measurements collected under known spectral stimuli (e.g., filter stacks or color patches) and a measured illuminant. In compact matrix form, this can be written as
% \begin{equation}
% R \;=\; t_{\mathrm{integ}}\, S^{\mathsf T}\, C \;+\; N,
% \label{eq:matrix_equation}
% \end{equation}
% where \(R \in \mathbb{R}^{K \times M}\) stacks the per-channel responses to \(M\) spectral stimuli, \(S \in \mathbb{R}^{N_\lambda \times K}\) collects the unknown sensitivity curves as columns \(\{s_i(\lambda)\}\) sampled on \(N_\lambda\) wavelengths, and \(C \in \mathbb{R}^{N_\lambda \times M}\) contains, column-wise, the spectral power distributions incident on the sensor for each stimulus. The matrix \(C\) is frequently ill-conditioned because practical filters exhibit broad, overlapping passbands; stable recovery of \(S\) thus relies on regularized inversion or constrained optimization (e.g., non-negativity and smoothness), and it presupposes that the measured \(R\) has been linearized to undo any static nonlinearity \cite{Uttner2006SpectralCameras,Cheung2004AccurateCameras}. Taken together, Equations~\eqref{eq:linear_response}–\eqref{eq:matrix_equation} provide the forward model and the estimation framework used throughout this work.


\noindent
Luminance is the photometric analogue of radiance and describes the luminous intensity per unit projected area in a given direction. Using luminous (photometric) quantities, it is defined as
\begin{equation}
L \;=\; \frac{\mathrm{d}^{2}\Phi}{\mathrm{d}A\,\cos\theta\,\mathrm{d}\Omega},
\end{equation}
where \(\Phi\) is the luminous flux, \(A\) is the source area, \(\Omega\) is the solid angle, and \(\theta\) is the angle between the surface normal and the line of sight. In imaging applications, luminance connects scene brightness to perceived contrast and exposure settings, but it also highlights an important distinction: photometric measures weight power by the human visual sensitivity function, whereas the camera models above are radiometric. Accurate, reproducible camera-based luminance measurements therefore require prior radiometric calibration, careful control of exposure and spectral response, and, where needed, a traceable conversion from radiometric to photometric units \cite{Wuller2007TheMeters}.\\


\subsection{Laboratory Procedure}
\label{sec:procedimiento_experimental_laboratorio}

\noindent This section details the laboratory procedure used for the spectral response characterization of various optical systems. The methodology includes the selection of the illumination source, validation of its spectrum, beam collimation, dark current correction of the minispectrometer, use of a set of filters and acrylic sheets, selection and combination of said filters, determination of the central wavelength and bandwidth, and measurement of optical power, followed by image capture with the optical system under test.

\subsubsection{Selection and validation of the illumination source}
\label{subsub:fuente_validacion}

\noindent A tungsten lamp from ThorLabs, model OSL-2~\cite{OSL2-Manual}, was selected as the main illumination source. This type of lamp emits radiation over a broad range, approximately from 400 nm to 1600 nm, which is suitable for covering both the visible and part of the near-infrared spectrum. To ensure the spectral distribution of the lamp matched expectations, a minispectrometer from Newport (Newport Miniespectrometer Oriel IS Series Model 78356) was used to measure its output spectrum. Figure~\ref{fig:lampara_tungsteno_espectro} shows a sample of the measured spectral signal, expressed in counts, which is the standard output format of such spectrometers. These counts represent the relative intensity recorded by the spectrometer's sensor, but do not constitute an absolute radiometric quantity. Therefore, to obtain a fully calibrated measurement, these counts must be related to the optical power measured by an independent power meter (Thorlabs Compact Power and Energy Meter Console PM100D).

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\textwidth]{Figures/C3/tunsteno.png}
    \caption{A sample of the measured spectrum of the tungsten lamp (OSL-2) using the minispectrometer. The vertical axis is expressed in counts, which depend on the intrinsic response of the spectrometer.}
    \label{fig:lampara_tungsteno_espectro}
\end{figure}

\subsubsection{Setup and Collimation of the Illumination Beam}
\label{subsub:montaje_colimacion}

The tungsten lamp includes a fiber optic coupling that allows directing the light toward various optical elements. The wavefront emerging from the fiber optic is divergent and non-punctual, so the optical power decreases significantly as the distance between the fiber output and the measurement plane increases. To work under controlled conditions and optimize power utilization, the light beam was collimated by mounting the fiber in a one-inch diameter lens tube. However, it is important to clarify that, due to the non-punctual nature of the source, a perfectly collimated wavefront cannot be obtained; residual divergence is always present.

\begin{enumerate}
    \item \textbf{Selection of the collimating lens:} A plano-convex lens with a focal length of $25$\,mm was selected. This lens was chosen because it was the shortest focal length option available in the laboratory with a 1-inch tube lens diameter, compatible with the optical setup. Although a longer focal length would reduce divergence, in practice, using lenses with longer focal lengths limits the effective light collection, thus decreasing the total power available in the collimated beam. A 25 mm lens provides greater optical field collection, even though at the cost of slightly more divergence.
        
    
    \item \textbf{Collimation method:}
    \begin{itemize}
        \item Traditionally, collimation is achieved by adjusting the lens position to minimize the angular divergence of the beam, which occurs when the lens is placed at a distance equal to its focal length $f$ from the illumination source. At this point, the emerging rays are approximately parallel and the beam diameter varies minimally along the propagation. However, in this work, this method was not used due to:
            \begin{enumerate}
            \item The lens used is spherical and the source (optical fiber) is not point-like, resulting in residual divergence and making it difficult to visually identify the optimal collimation position.
            \item The main objective is to maximize the usable optical power at the measurement plane, so characterizing beam uniformity solely by observing the diameter is not efficient.
            \end{enumerate}

            \item Instead, an optical power meter is positioned at a fixed distance from the collimating lens (see Figure~\ref{fig:montaje_colimacion}).
            \item A bandpass filter with the narrowest possible bandwidth is placed to approximate the output to a quasi-monochromatic source. In this experiment, a filter centered at 633 nm with a bandwidth of 4 nm was used. This allows the optical powermeter to be configured with a well-defined central wavelength, a necessary condition for obtaining an accurate measurement of the transmitted power.
            \item The distance between the fiber and the lens is varied around the nominal focal length (25\,mm), seeking the maximum optical power measured at the detector. This point corresponds to the configuration with minimal beam divergence near the measurement plane and maximum power collection. Additionally, care is taken to ensure that the entire area of the detector is fully illuminated by the beam, as this is a necessary condition for obtaining an accurate irradiance measurement based on the illuminated area of the detector, which, in the case of complete illumination, corresponds to the total detector area.
    \end{itemize}
    
    \item \textbf{Final positioning:} Once the position that maximizes the detected power is found, the positions of the fiber and the lens are mechanically fixed. Additionally, the distance between the lens and the power meter is kept constant for subsequent measurements, ensuring consistent illumination conditions.
\end{enumerate}

\begin{figure}[h!]
    \centering
    \includegraphics[trim=30mm 0mm 30mm 25mm, clip, width=0.9\textwidth]{Figures/C3/colimacion.pdf}
    \caption{Setup for collimating the beam of the tungsten lamp with fiber optics. The adjustment of the distance between the fiber and the plano-convex lens aims to maximize the optical power measured by the detector.}
    \label{fig:montaje_colimacion}
\end{figure}

\subsubsection{Dark Current Correction in the Minispectrometer}
\label{subsub:corriente_oscura_espectrometro}

The minispectrometer used to validate the light source spectrum and measure the transmissions of different filters records a base signal even without illumination, known as dark current or electronic noise. This is an offset of the measurement that needs to be quantified for proper corrections. To improve the accuracy of spectral measurements, the following procedure was applied:\\

\begin{itemize}
    \item A reference measurement was taken under total absence of light. For this, the detector of the minispectrometer was covered with its cap, and both the illumination source and ambient lighting were turned off to avoid any unwanted light during the measurement. This signal is denoted by \(\mathbf{D}\) and corresponds to the electronic noise level.
    
    \item For any measured spectrum \(\mathbf{S}\) (e.g., the spectrum of the tungsten lamp and a specific filter), correction is applied by subtracting \(\mathbf{D}\):
    \begin{equation}    
        \mathbf{S}_{\text{corr}} \;=\; \mathbf{S} \;-\; \mathbf{D}.
    \end{equation}
    
\end{itemize}

\noindent This attenuates the offset due to readout noise and provides a more reliable reading of the spectral distribution in counts. Figure~\ref{fig:dark_current_measurement} shows the measured dark current spectrum of the minispectrometer, acquired under complete absence of light. The mean value of the dark current was found to be 3513.97 counts. This is a significant offset, considering that the maximum possible reading of the spectrometer is 55,000 counts. Consequently, the dark current introduces a minimum error of 6.4 \% in the measurements, calculated as the ratio of the mean dark current to the maximum count value. This value represents the lower bound of the error induced by dark current; in practice, the relative error can be even higher, especially for measurements with lower signal levels. Therefore, proper dark current correction is essential to ensure the accuracy and reliability of all subsequent spectral measurements.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{Figures/C3/DC_measured_MS.png}
    \caption{Measured dark current spectrum of the minispectrometer. The mean value is 3513.97 counts, which represents a significant offset relative to the instrument's dynamic range.}
    \label{fig:dark_current_measurement}
\end{figure}

\subsubsection{Set of Filters and Acrylic Sheets}
\label{subsub:filtros_laminas}

In an ideal scenario, the appropriate instrument for spectral characterization is a monochromator, which allows precise filtering of the illumination to produce a quasi-monochromatic beam at any wavelength within the operational range of the device, with a bandwidth as narrow as 1 nm. Such a device enables highly accurate measurement of the system's spectral response by providing tunable, narrowband excitation. However, a monochromator is not available in the optics laboratory; therefore, the characterization must be performed using the available set of optical filters and acrylic sheets. The following section describes the empirical method developed to approximate the spectral response under these constraints.
\noindent The optical elements used in this experiment consist of two sets:
\begin{itemize}
    \item \textbf{Nine high-quality optical filters} mountable in one-inch tubes. These filters are numbered from 0 to 6, with two additional filters labeled 532 and 633 (corresponding to their central wavelengths). The selection of optical filters was based solely on the availability in the optics laboratory where the experiment was conducted.
    
    \item \textbf{Eight acrylic sheets} of different colors, each 2\,x\,2\,inches, mountable in an optical slide holder. The choice of colors was determined by the availability from local acrylic suppliers in Medellín; therefore, the largest possible variety of different colors was selected, based solely on what was accessible in the local market.
\end{itemize}

\noindent Figure~\ref{fig:laminas_filtros} shows the filters and acrylic sheets with their respective labels. The transmitted spectrum of each element was measured (using the tungsten lamp as the illumination source) with the minispectrometer. These measurements were corrected for dark current as described in Section~\ref{subsub:corriente_oscura_espectrometro}.

\begin{figure}[H]
    \centering
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{Figures/C3/laminas.pdf}
        \caption{Set of acrylic sheets used. Each is individually labeled based on its apparent color.}
        \label{fig:laminas}
    \end{subfigure}
    \vspace{1em}
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{Figures/C3/filtros.pdf}
        \caption{Set of filters used. Filters are numbered 0 to 6, with two additional filters labeled 532 and 633.}
        \label{fig:filtros}
    \end{subfigure}
    \caption{Acrylic sheets and filters used in the experiment.}
    \label{fig:laminas_filtros}
\end{figure}

\subsubsection{Measurement of Transmission Spectra}
\label{subsub:medicion_transmision}

The transmission spectra of each filter and acrylic sheet are measured with the Miniespectrometer to determine their effective spectral transmission. Figure~\ref{fig:montaje_spec} shows the optical setup used to measure the transmission spectrum.

\begin{figure}[H]
    \centering
    \includegraphics[trim=30mm 0mm 40mm 25mm, clip, width=0.9\textwidth]{Figures/C3/montaje_spec.pdf}
    \caption{Setup for measuring the transmission spectrum of filters and acrylic sheets.}
    \label{fig:montaje_spec}
\end{figure}


\noindent For the illumination, the base spectrum \(\mathbf{I}\) is obtained (dark-current corrected). For each filter or sheet, the transmitted spectrum \(\mathbf{T}_j\) is measured, also corrected for dark current. To obtain the dimensionless relative spectral transmittance of each element, the transmitted spectrum \(\mathbf{T}_j\) is divided by the reference spectrum \(\mathbf{I}\). This approach is justified because, for a linear optical system, the transmission process can be modeled as the product of the source spectrum and the spectral transfer function of the element (filter or sheet):

\begin{equation}
    \mathbf{T}_j \;=\; \mathbf{I} \;\times\; \beta_j(\lambda),
\end{equation}
\begin{equation}
    \frac{\mathbf{T}_j}{\mathbf{I}} \;=\; \beta_j(\lambda).
\end{equation}

\noindent The measured spectra for each filter and acrylic sheet are presented in Figure~\ref{fig:espectros_combinados}. Thus, the curve \(\beta_j(\lambda)\) represents the effective spectral transmittance of the filter or sheet (see Figure~\ref{ig:filtros_laminas_espectros_relativos}).

\begin{figure}[H]
    \centering
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=0.9\textwidth]{Figures/C3/filtros_spec.png}
        \caption{Filters.}
        \label{fig:filtros_espectros}
    \end{subfigure}
    \vspace{1em}
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=0.9\textwidth]{Figures/C3/laminas_spec.png}
        \caption{Acrylic plates.}
        \label{fig:laminas_espectros}
    \end{subfigure}
    \caption{Measured spectra (in counts) of tungsten light transmitted through various filters (A) and acrylic plates (B). The vertical axis shows the relative intensity recorded by the minispectrometer in counts. The spectrum of filter 532 and 633 are scaled to match the maximum of the tungsten lamp spectrum.}
    \label{fig:espectros_combinados}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=0.9\textwidth]{Figures/C3/filtros_respuestaspec.png}
        \caption{Filters.}
        \label{fig:filtros_espectros_resp}
    \end{subfigure}
    \vspace{1em}
    \begin{subfigure}{\textwidth}
        \centering
        \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=0.9\textwidth]{Figures/C3/laminas_respuestaspec.png}
        \caption{Acrylic plates.}
        \label{fig:laminas_espectros_resp}
    \end{subfigure}
    \caption{Normalized dimensionless spectral response (transmittance) obtained by dividing each filtered spectrum by the tungsten lamp spectrum.}
    \label{ig:filtros_laminas_espectros_relativos}
\end{figure}

\subsubsection{Generation and Selection of Filter Combinations}
\label{subsub:combinaciones_filtros}

\noindent Given that the filters have relatively wide bandwidths (many exceeding 100 nm), this work evaluates the possibility of combining up to three filters or acrylic plates to obtain more selective transmission curves. Given that the method used is computational, a Python script was developed to perform the following tasks: 

\begin{enumerate}
    \item Lists all possible combinations of:
    \begin{itemize}
        \item Individual filters and/or acrylic sheets,
        \item Pairs of filters/sheets,
        \item Triplets of filters/sheets.
    \end{itemize}
    \item For each combination, computes the \emph{composite transmission curve} \(\beta_{\text{comp}}(\lambda)\) by multiplying the individual transmission curves of each filter or acrylic plate as a function of wavelength. This models the sequential attenuation of light as it passes through each optical element as a convolution of the optic field with the optics elements.
    \item Generates a total of 987 combinations (including single, double, and triple combinations).
\end{enumerate}

\paragraph{Ideal Transmission Curves.}
To guide the selection process, 10 target curves are defined, modeled as Gaussian functions with a standard deviation of 3\,nm:

\begin{itemize}
    \item Seven curves centered in the visible spectrum: 400, 450, 500, 550, 600, 650, and 700\,nm.
    \item Three curves for the near-infrared (NIR): 825, 850, and 875\,nm.
\end{itemize}

Each \(\beta_{\text{ideal}}(\lambda)\) adopts the form:
\[
\beta_{\text{ideal}}(\lambda) \;=\; \exp\!\Big[-\tfrac{(\lambda - \lambda_0)^2}{2 \,\sigma^2}\Big],
\]
where \(\lambda_0\) is the central wavelength and \(\sigma=3\) nm.  
Figure~\ref{fig:curvas_ideales} illustrates the ideal profiles used.

\begin{figure}[H]
    \centering
    \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=1\textwidth]{Figures/C3/ideal_spec.png}
    \caption{Ideal transmission curves for selecting filter combinations. This example shows 10 Gaussians centered at 400, 450, 500, 550, 600, 650, 700, 825, 850, and 875 nm, respectively.}
    \label{fig:curvas_ideales}
\end{figure}

\paragraph{Selection Metric.}
To determine which filter combination best matches the defined ideal curves, the Pearson correlation coefficient is used. Given an ideal curve \(\beta_{\text{ideal}}(\lambda)\) and a composite curve \(\beta_{\text{comp}}(\lambda)\), both discretized in \(N\) spectral points, the Pearson correlation coefficient \(r\) is defined as:

\begin{equation}
r(\beta_{\text{ideal}}, \beta_{\text{comp}}) =
\frac{
    \displaystyle \sum_{i=1}^{N} 
    \left[\beta_{\text{ideal}}(\lambda_i) - \bar{\beta}_{\text{ideal}}\right]
    \left[\beta_{\text{comp}}(\lambda_i) - \bar{\beta}_{\text{comp}}\right]
}{
    \sqrt{
        \left(
        \displaystyle \sum_{i=1}^{N} 
        \left[\beta_{\text{ideal}}(\lambda_i) - \bar{\beta}_{\text{ideal}}\right]^2
        \right)
        \left(
        \displaystyle \sum_{i=1}^{N} 
        \left[\beta_{\text{comp}}(\lambda_i) - \bar{\beta}_{\text{comp}}\right]^2
        \right)
    }
}
\end{equation}

where:
\begin{itemize}
    \item \(\beta_{\text{ideal}}(\lambda_i)\) is the ideal transmission at wavelength \(\lambda_i\),
    \item \(\beta_{\text{comp}}(\lambda_i)\) is the composite transmission of the filter combination at \(\lambda_i\),
    \item \(\bar{\beta}_{\text{ideal}}\) and \(\bar{\beta}_{\text{comp}}\) are the mean values of each curve across the evaluated range. Since the curves ara Gaussians, this is the central wavelength of the ideal curve and the mean of the composite curve, respectively.
\end{itemize}

\noindent The Pearson coefficient ranges from \([-1, 1]\). A value close to 1 indicates high similarity in shape and relative distribution; values near 0 or negative indicate low similarity. Therefore, the combination with the highest \(r\) is selected as the “optimal” match for a given target curve.\\

\medskip

\noindent
\textbf{Selection Procedure:}
\begin{enumerate}
    \item For each ideal curve \(\beta_{\text{ideal}}(\lambda)\), all composite curves \(\beta_{\text{comp}}(\lambda)\) are evaluated.
    \item \(r(\beta_{\text{ideal}}, \beta_{\text{comp}})\) is computed over the same wavelength range.
    \item The combination that maximizes \(r\) is selected.
\end{enumerate}

\noindent This method allows identifying the filter combination that best approximates a desired spectral shape. For each central wavelength (e.g., 400, 450, 500, …, 700, 825, 850, and 875\,nm), an “optimal” combination is thus obtained. Figure~\ref{fig:best_corr_combinations} shows the best-matching filter combinations overlaid on their corresponding ideal curves.

\begin{figure}[H]
    \centering
    \includegraphics[trim=0mm 0mm 0mm 25mm, clip, width=1\textwidth]{Figures/C3/best_matches.png}
    \caption{Filter combinations (solid lines) with the highest correlation to their corresponding ideal curves (dashed lines). The shape and peak position of the selected combinations match the target profiles.}
    \label{fig:best_corr_combinations}
\end{figure}

\medskip

\noindent Additionally, some combinations are selected experimentally, since the correlation-based method does not consider absolute transmittance. It is possible that the combination with the highest correlation \(r\) has very low transmittance, limiting its practical utility. Also, individual filters such as those centered at 532 and 633\,nm show good spectral performance. By combining the maximum correlation criterion with experimental validation, the final set of filters used for spectral characterization is selected.

\begin{figure}[htbp]
    \centering
    \includegraphics[trim= 0mm 0mm 0mm 20mm, clip, width=1\textwidth]{Figures/C3/filt_select.png}
    \caption{Final filter selection used for spectral characterization of the optical systems. Includes both experimentally selected configurations and individual filters (532 and 633 nm) showing good correlation and transmittance.}
    \label{fig:selected_filters}
\end{figure}

\subsubsection{Determination of experimental central wavelength, bandwidth, and optical power of selected combinations}
\label{subsub:longitud_onda_central_potencia}

After defining the optimal combinations, the actual transmission spectra are measured using the minispectrometer to verify the effective central wavelength, resulting bandwidth, and transmitted optical power. This is essential to:

\begin{enumerate}
    \item Properly configure the optical power meter (which requires the central wavelength of the incoming radiation).
    \item Verify that the actual transmission curves are consistent with the numerically determined ones.
\end{enumerate}

\noindent The optical power transmitted by each selected combination is then measured using a power meter. To maintain consistency across measurements, the tungsten lamp operates at its maximum irradiance capacity. For each combination, the power is measured at the previously determined central wavelength. The lowest power value among all combinations is used as the reference. This ensures that subsequent optical system measurements are performed under the same irradiance conditions, adjusting the power of the lamp or using neutral density filters to reduce irradiance in cases where power exceeds this reference.\\

\medskip

\noindent The bandwidth of the measured spectra for each selected filter is also calculated. Each spectrum is first normalized to have a maximum value of 1. The bandwidth is defined as the spectral interval where the signal reaches at least 50\% of its maximum value (i.e., the Full Width at Half Maximum, FWHM). For filters with multiple peaks, the main peak of interest is selected, usually corresponding to the expected region (e.g., NIR). A Python script is developed that:

\begin{enumerate}
    \item Identifies the maximum peak of each normalized spectrum.
    \item Determines the wavelengths \(\lambda_{\text{left}}\) and \(\lambda_{\text{right}}\) where the spectral response falls to 50\% of the maximum.
    \item Calculates the bandwidth as:
    \[
    \text{FWHM} = \lambda_{\text{right}} - \lambda_{\text{left}}.
    \]
\end{enumerate}

\noindent Figure \ref{fig:bandwidth_central} shows the central wavelength and FWHM of the selected filters, obtained through the described procedure. These parameters are key to assessing the system’s ability to discriminate spectral bands and for configuring the power meter accurately.

\begin{figure}[H]
    \centering
    \includegraphics[trim=0mm 0mm 50mm 25mm, clip, width=1\textwidth]{Figures/C3/ancho de banda.png}
    \caption{Example of a normalized spectrum of a selected filter. The central wavelength and bandwidth (FWHM) are indicated, defined as the spectral range where the signal reaches 50\% of its maximum value.}
    \label{fig:bandwidth_central}
\end{figure}

\noindent
Table~\ref{tab:cwl_fwhm_sorted} lists the experimental centre wavelength (\(\lambda_{\mathrm{c}}\)) and full width at half-maximum (FWHM) obtained for every filter combination.  
The set spans the entire visible region and a portion of the NIR: the blue edge is defined by the \textit{5 × Purple} stack at \(\lambda_{\mathrm{c}} = 427\;\mathrm{nm}\), while the longest wavelength appears in the \textit{2 × 5} pair (\(\lambda_{\mathrm{c}} = 839\;\mathrm{nm}\)).  
Two single-element interferential filters, 532 nm and 633 nm, deliver the narrowest pass-bands (4.59 nm and 5.05 nm, respectively), providing quasi-monochromatic illumination that is particularly useful for validating the wavelength calibration of the camera.  
Conversely, the widest profile corresponds to the \textit{1 × Purple × Red} combination, whose 77 nm FWHM sacrifices spectral selectivity in favour of higher photon flux—an advantageous trade-off when signal-to-noise is critical.  
Most composite stacks in the 800–830 nm range (\textit{2 × 5}, \textit{3 × 4 × Purple} and \textit{4 × Red}) cluster around 820–840 nm with FWHM near 18–20 nm, offering complementary NIR bands that retain adequate power while limiting overlap.  
Taken together, the ensemble of filters therefore supplies a graded palette of bandwidths—from sub-5 nm to ~80 nm—allowing the experimental protocol to tailor spectral resolution and radiant power to the requirements of each calibration step.\\


\begin{table}[H]
    \centering
    \caption{Filter combinations ordered by centre wavelength.}
    \label{tab:cwl_fwhm_sorted}
    \begin{tabular}{|l|c|c|}
        \hline
        \rowcolor[HTML]{EFEFEF}
        \textbf{Filter combination} & \(\lambda_{\mathrm{c}}\) [nm] & FWHM [nm] \\ \hline
        5 × Purple                  & 427.07 & 42.55 \\ \hline
        5 × Green                   & 501.15 & 30.24 \\ \hline
        5 × 6 × Dark Green          & 509.78 & 17.57 \\ \hline
        4 × Dark Blue               & 520.59 & 35.72 \\ \hline
        532 nm                      & 532.16 & 4.59  \\ \hline
        4 × Orange                  & 574.43 & 26.73 \\ \hline
        2 × 3 × Yellow              & 605.39 & 20.21 \\ \hline
        633 nm                      & 632.19 & 5.05  \\ \hline
        1 × Purple × Red            & 701.89 & 77.16 \\ \hline
        4 × Red                     & 822.38 & 20.19 \\ \hline
        3 × 4 × Purple              & 826.43 & 18.98 \\ \hline
        2 × 5                       & 839.43 & 17.14 \\ \hline
    \end{tabular}
\end{table}

\noindent
Table~\ref{tab:optical_power} summarises the optical power delivered by each filter combination when the tungsten lamp operates at full intensity.  
The highest throughput corresponds to the \textit{5~$\times$~Purple} stack, which transmits \(70.4\;\mathrm{mW}\), whereas the narrowband 532\,nm filter allows only \(0.42\;\mathrm{mW}\) to reach the detector, making it the most attenuating element in the set. To ensure a homogeneous spectral response during camera characterization, the experiment adopts the lowest recorded value—\(0.42\;\mathrm{mW}\)—as the reference irradiance. For every other combination, the lamp output is reduced and, when necessary, neutral-density filters are inserted until the incident power equals this reference. This strategy removes irradiance as a confounding variable, so that the measured sensor counts depend solely on the relative spectral response of the imaging chain.\\

\begin{table}[H]
    \centering
    \caption{Optical power measured for each filter combination (tungsten lamp at maximum setting).}
    \label{tab:optical_power}
    \begin{tabular}{|l|c|}
        \hline
        \rowcolor[HTML]{EFEFEF}
        \textbf{Filter combination} & \textbf{Power [mW]} \\ \hline
        5 $\times$ Purple               & 70.4 \\ \hline
        1 $\times$ Purple $\times$ Red  & 68.1 \\ \hline
        5 $\times$ Green                & 53.3 \\ \hline
        4 $\times$ Orange               & 43.1 \\ \hline
        2 $\times$ 3 $\times$ Yellow    & 36.8 \\ \hline
        4 $\times$ Red                  & 26.5 \\ \hline
        2 $\times$ 5                    & 26.2 \\ \hline
        4 $\times$ Dark Blue            & 23.7 \\ \hline
        5 $\times$ 6 $\times$ Dark Green& 17.9 \\ \hline
        3 $\times$ 4 $\times$ Purple    & 9.7  \\ \hline
        633\,nm                         & 1.47 \\ \hline
        532\,nm                         & 0.42 \\ \hline
    \end{tabular}
\end{table}

\subsubsection{Mesaurement of the optical system response}
\label{subsub:captura_sistemas_opticos}


\noindent
After selecting the filter combinations and setting the irradiance to the reference value, each camera under test—an RGB unit and a monochannel NIR sensor—was characterised sequentially.  
The procedure began by configuring the sensor electronics: the analogue gain was fixed at unity to avoid scale factors between captures; the gamma curve was set to one (linear) to suppress any internal non-linearity; and, in the RGB camera, the red and blue white-balance coefficients were also forced to one (the green channel is unity by default), thereby preventing firmware colour corrections. The exposure time was then adjusted so that no pixel reached saturation; its value was recorded because, under the linear-response model established in Equation~\eqref{eq:linear_response}, the total signal is directly proportional to the integration time.\\


\noindent
Whenever the measured optical power at the detector exceeded the \(0.42\;\mathrm{mW}\) reference, a neutral-density filter was inserted in the one-inch lens tube downstream of the collimator.  
This ensured that every filter stack illuminated the sensor with the same radiant power.\\

\noindent
For each filter combination the camera acquired fifty successive frames in an uncompressed format (\texttt{.npy}).  
A second series of fifty dark frames was collected with the objective completely covered, yielding a master dark image \(\mathbf{D}_{\text{prom}}\) that represents the electronic offset.  
Subtracting \(\mathbf{D}_{\text{prom}}\) from every illuminated frame removed the dark current contribution; the corrected frames were averaged to produce a single, low-noise image \(\mathbf{I}_{\text{filt}}\) corresponding to that particular filter stack (see Section~\ref{subsub:corriente_oscura_espectrometro}). \\


\noindent
The spectral response was finally derived from \(\mathbf{I}_{\text{filt}}\).  
Within a predefined region of interest the digital counts were summed to obtain a scalar response metric.  
In the RGB camera this sum was calculated independently for the R, G and B channels, whereas in the NIR system the single channel was used.  
Each metric was paired with the previously determined centre wavelength \(\lambda_{0}\) of the corresponding filter combination.  
Repeating this workflow across all filters produced a discrete map of the optical system’s spectral response, suitable for calibrating the relative spectral sensitivity of both cameras.\\

\begin{equation}
    R(\lambda_{0}) \;=\;
    \sum_{x=0}^{W-1}\sum_{y=0}^{H-1}
    I_{\text{filt}}\bigl(x,\,y;\,\lambda_{0}\bigr),
    \label{eq:spectral_response_metric_full}
\end{equation}

\noindent
Here \(R(\lambda_{0})\) is the scalar response associated with the filter’s centre wavelength \(\lambda_{0}\); the summation extends over every pixel \((x,y)\) inside the image, where $W$ and $H$ are the image width and height in pixels, and \(I_{\text{filt}}(x,y;\lambda_{0})\) denotes the dark-corrected, noise-averaged image obtained with that filter. For the RGB camera the same expression is applied independently to each colour plane \(k\in\{R,G,B\}\) by replacing \(I_{\text{filt}}\rightarrow I_{\text{filt},k}\); in the NIR system it is evaluated once on the single monochromatic channel.



\section{Main contributions}
%---------------------------------------------------------------
%===============================================================
\subsection{Field of View (FOV) Characterization Results}
\label{sec:fov_results}
%===============================================================

\noindent To validate the field of view of the selected optical systems (System 7 VIS and System 6 NIR), horizontal (H) and vertical (V) FOV measurements were performed at three working distances ($\mathrm{WD}$) using the ISO 12233 test chart. The results obtained are presented below.\\

\noindent Figure \ref{fig:fov_montage} shows the 12 original captures (3 rows x 4 columns) used to experimentally determine the FOV. Each row corresponds to a different working distance; the first two columns are the H and V measurements for VIS (System 7) and the last two are for NIR (System 6).\\

\begin{figure}[H]
\centering
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{0}
\begin{tabular}{cccc}
    \multicolumn{2}{c}{\small NIR – System 6} &
    \multicolumn{2}{c}{\small VIS – System 7} \\
    H & V & H & V \\ 

    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/NIR/H/7_1.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/NIR/V/8_7.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/H/9_2.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/V/10_7.png} \\
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/NIR/H/33_1.png} &
    \includegraphics[trim= 6cm 6cm 6cm 6cm, clip, width=.22\linewidth]{Figures/C4/FOV/NIR/V/65.png} & % TOCLIP
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/H/37_8.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/V/30_3.png} \\
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/NIR/H/63.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/NIR/V/65.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/H/69_4.png} &
    \includegraphics[width=.22\linewidth]{Figures/C4/FOV/VIS/V/70.png} \\
\end{tabular}
\caption{Original images for the measurement of horizontal (H) and vertical (V) FOV at three working distances.}
\label{fig:fov_montage}
\end{figure}

\noindent The measurements, converted to meters, are summarized in Table \ref{tab:fov_raw_wd}, where each FOV is accompanied by an instrumental uncertainty of $\sigma=1$ mm.\\

\begin{table}[H]
\centering
\caption{Measured working distance WD and FOV for each optical system and orientation.}
\label{tab:fov_raw_wd}
\small
\setlength{\tabcolsep}{4pt} % More compact column spacing
\renewcommand{\arraystretch}{1.2} % Row spacing
\begin{tabularx}{\textwidth}{|c|>{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X|
                                    >{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X
                                    >{\centering\arraybackslash}X|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    & \multicolumn{4}{c|}{\textbf{NIR (Sys.~6)}} 
    & \multicolumn{4}{c|}{\textbf{VIS (Sys.~7)}} \\ \cline{2-9}
    \rowcolor[HTML]{EFEFEF}
    \textbf{Test} 
    & WD$_H$ [m] & FOV$_H$ [m] & WD$_V$ [m] & FOV$_V$ [m] 
    & WD$_H$ [m] & FOV$_H$ [m] & WD$_V$ [m] & FOV$_V$ [m] \\ 
    \hline
    1 & 0.071 & 0.0350 & 0.087 & 0.0350 & 0.092 & 0.0350 & 0.107 & 0.0350 \\
    2 & 0.331 & 0.231  & 0.350 & 0.180  & 0.379 & 0.239  & 0.303 & 0.138  \\
    3 & 0.650 & 0.468  & 0.650 & 0.349  & 0.694 & 0.458  & 0.700 & 0.342  \\ 
    \hline
\end{tabularx}
\end{table}

\noindent Applying weighted linear regression (Deming) with weights $w=1/\sigma$, the optimal fits were:\\

\begin{equation}
\begin{aligned}
\text{VIS:}\;&\mathrm{FOV_H}=(0.7025\pm0.0045)\,\mathrm{WD}-(0.0288\pm0.0021),\\
\text{VIS:}\;&\mathrm{FOV_V}=(0.5171\pm0.0029)\,\mathrm{WD}-(0.0197\pm0.0013),\\
\text{NIR:}\;&\mathrm{FOV_H}=(0.7477\pm0.0031)\,\mathrm{WD}-(0.0175\pm0.0013),\\
\text{NIR:}\;&\mathrm{FOV_V}=(0.5579\pm0.0034)\,\mathrm{WD}-(0.0141\pm0.0015).
\end{aligned}
\end{equation}

\noindent Figure \ref{fig:fov_reg_combined} illustrates the fits with their $1\sigma$ confidence bands.\\

\begin{figure}[H]
\centering
\includegraphics[width=1\linewidth]{Figures/C4/FOV.png}
\caption{Weighted linear fit of FOV vs. WD for the four cases: VIS–H, VIS–V, NIR–H, and NIR–V.}
\label{fig:fov_reg_combined}
\end{figure}

\noindent To quantify the deviation between the empirical models and the theoretical predictions, Table \ref{tab:fov_pred_vs_theory_HV} compares the FOV values at key operational altitudes.\\

\begin{table}[H]
\centering
\caption{Comparison of empirical vs. theoretical for horizontal (H) and vertical (V) FOV.}
\label{tab:fov_pred_vs_theory_HV}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\columnwidth}{|c| *{8}{>{\centering\arraybackslash}X|}}
\toprule
& \multicolumn{4}{c|}{\textbf{VIS} (Sys.~7)} 
& \multicolumn{4}{c|}{\textbf{NIR} (Sys.~6)} \\
\midrule
\textbf{Altitude}
& \shortstack{Pred H\\(m)} 
& \shortstack{Theo H\\(m)} 
& \shortstack{Pred V\\(m)} 
& \shortstack{Theo V\\(m)} 
& \shortstack{Pred H\\(m)} 
& \shortstack{Theo H\\(m)} 
& \shortstack{Pred V\\(m)} 
& \shortstack{Theo V\\(m)} \\
\midrule
50\,m    
& 35.10 & 33.54 & 25.84 & 25.16 
& 37.37 & 35.64 & 27.88 & 26.73 \\
1000\,m 
& 702.50 & 670.87 & 517.08 & 503.15 
& 747.70 & 712.80 & 557.90 & 534.60 \\
5000\,m 
& 3512.00 & 3354.35 & 2585.48 & 2515.76 
& 3737.00 & 3564.00 & 2788.89 & 2673.00 \\
\bottomrule
\end{tabularx}
\end{table}


\noindent The absolute discrepancy ($\Delta_abs$) and relative discrepancy ($\Delta_rel$) between the predicted ($FOV_pred$) and theoretical ($FOV_teo$) values are defined as:

\begin{equation}
\Delta_{\mathrm{abs}} = FOV_{\mathrm{pred}} - FOV_{\mathrm{theo}}, 
\qquad
\Delta_{\mathrm{rel}} = \frac{\Delta_{\mathrm{abs}}}{FOV_{\mathrm{teo}}} \times 100\%.
\end{equation}

\noindent Figure~\ref{fig:discrepancies_vs_alt}\subref{fig:abs_error_vs_alt} shows the absolute discrepancy for horizontal and vertical FOVs at the three working altitudes, whereas Figure~\ref{fig:discrepancies_vs_alt}\subref{fig:rel_error_vs_alt} presents the corresponding relative discrepancy. In each panel, four curves are plotted (VIS–H, VIS–V, NIR–H, NIR–V). The results indicate that, while the absolute discrepancy grows approximately linearly with altitude, the relative discrepancy remains nearly constant with altitude—about \(4.6\text{–}4.9\,\%\) in the horizontal direction and \(2.7\text{–}2.8\,\%\) (VIS–V) or \(4.3\text{–}4.9\,\%\) (NIR–V) in the vertical direction. This altitude-independent bias is consistent with a small, systematic geometric effect (e.g., residual lens distortion or parallax in the working-distance measurement) that should be corrected prior to orthomosaic generation.


\begin{figure}[H]
\centering
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/C4/absolute_error_plot_fov.png}
\caption{Absolute discrepancy $\Delta_{\mathrm{abs}}$ versus altitude.
The absolute error increases with altitude because the difference between the predicted and theoretical FOV grows.}
\label{fig:abs_error_vs_alt}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.49\textwidth}
\centering
\includegraphics[width=\linewidth]{Figures/C4/relative_error_plot_fov.png}
\caption{Relative discrepancy $\Delta_{\mathrm{rel}}$ versus altitude.
Despite increasing absolute errors, the relative error remains essentially constant with altitude, indicating a fixed geometric bias.}
\label{fig:rel_error_vs_alt}
\end{subfigure}
\caption{Comparison of the absolute and relative discrepancies between the predicted and theoretical FOVs for the VIS and NIR systems as a function of altitude.}
\label{fig:discrepancies_vs_alt}
\end{figure}


\subsection{Angular Characterization: AFOV vs. WD}

\noindent The experimental Angular Field of View (AFOV) was determined at three distinct working distances (WD) for both the VIS (System 7) and NIR (System 6) setups. The results are summarized in Table~\ref{tab:afov_exp}. Each AFOV value was calculated from the direct measurements of the linear Field of View (FOV) and the working distance (WD) using the fundamental trigonometric relationship:

\begin{equation}
\label{eq:afov_experimental}
\mathrm{AFOV}_{\mathrm{exp}} = 2 \cdot \arctan\left(\frac{\mathrm{FOV}}{2 \cdot \mathrm{WD}}\right)
\end{equation}

\begin{table}[H]
\centering
\caption{Experimental AFOV for WD of 0.07–0.09 m, 0.30–0.38 m, and 0.65–0.70 m.}
\label{tab:afov_exp}
\begin{tabular}{lccc}
    \toprule
    System & WD [m] & AFOV$_H$ [°] & AFOV$_V$ [°] \\
    \midrule
    \textbf{VIS (System 7)} 
    & 0.09 & 21.54 & 18.58 \\
    & 0.38 & 35.00 & 25.66 \\
    & 0.69 & 36.52 & 27.46 \\
    \midrule
    \textbf{NIR (System 6)} 
    & 0.07 & 27.69 & 22.75 \\
    & 0.33 & 38.47 & 28.84 \\
    & 0.65 & 39.60 & 30.05 \\
    \bottomrule
\end{tabular}
\end{table}

\noindent To validate these measurements, the experimental AFOV at the maximum working distance was compared against the theoretical AFOV, which is derived from the system's design parameters (sensor size and focal length, $f$) and represents the ideal performance at an infinite working distance. Based on the system specifications, the theoretical values are: $37.1^\circ$ (VIS-H), $28.24^\circ$ (VIS-V), $39.3^\circ$ (NIR-H), and $29.93^\circ$ (NIR-V).\\

\noindent The absolute ($\Delta_{\rm abs}$) and relative ($\Delta_{\rm rel}$) discrepancies were defined to quantify the deviation from the theoretical model:

\begin{equation}
\label{eq:discrepancy}
\Delta_{\mathrm{abs}} = \mathrm{AFOV}_{\mathrm{exp}} - \mathrm{AFOV}_{\mathrm{theo}},
\qquad
\Delta_{\mathrm{rel}} = \frac{|\Delta_{\mathrm{abs}}|}{\mathrm{AFOV}_{\mathrm{theo}}} \times 100\%.
\end{equation}

\noindent The resulting discrepancies, calculated at the largest measured WD, were as follows:

\begin{align*}
\text{Horizontal:}\quad
\Delta_{\rm abs}^{\rm VIS\text{-}H} &= 36.52^\circ - 37.10^\circ = -0.58^\circ\quad(\Delta_{\rm rel}\approx1.6\%) \\
\Delta_{\rm abs}^{\rm NIR\text{-}H} &= 39.60^\circ - 39.30^\circ = +0.30^\circ\quad(\Delta_{\rm rel}\approx0.8\%) \\[6pt]
\text{Vertical:}\quad
\Delta_{\rm abs}^{\rm VIS\text{-}V} &= 27.46^\circ - 28.24^\circ = -0.78^\circ\quad(\Delta_{\rm rel}\approx2.8\%) \\
\Delta_{\rm abs}^{\rm NIR\text{-}V} &= 30.05^\circ - 29.93^\circ = +0.12^\circ\quad(\Delta_{\rm rel}\approx0.4\%)
\end{align*}

\noindent The convergence of the experimental AFOV towards the theoretical limit as the working distance increases is illustrated graphically in Figure~\ref{fig:afov}. In the VIS case, the AFOV grows towards the theoretical value without exceeding it. Conversely, for the NIR system, the experimental value slightly surpasses the theoretical one, which may suggest a slightly misadjusted focus or other systematic effects.\\

\begin{figure}[H]
\centering
\begin{subfigure}[b]{.9\linewidth}
    \includegraphics[width=\linewidth]{Figures/C4/afov_nir.png}
    \caption{NIR – System 6}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.9\linewidth}
    \includegraphics[width=\linewidth]{Figures/C4/afov_vis.png}
    \caption{VIS – System 7}
\end{subfigure}
\caption{Experimental AFOV vs. WD (dotted line indicates the theoretical infinite focus value).}
\label{fig:afov}
\end{figure}

\noindent The convergence behavior visible in the graphs is a key finding. Equation~\ref{eq:afov} defines the AFOV in an infinite focus regime, which is the expected operational condition for high-altitude imaging. The laboratory measurements demonstrate that as the working distance increases, the experimental AFOV rapidly approaches this theoretical limit. In fact, at the maximum distance measured in the lab (~0.7 m), the discrepancy is already minimal. This indicates that the system behaves predictably and that the theoretical model is a robust predictor for the mission's operational distances (e.g., 50 m or more), where the system will effectively operate under "infinite focus" conditions.\\

\noindent The residual discrepancies, though small, are significant for high-precision applications and are primarily attributed to sources of systematic error. The most prominent of these is geometric lens distortion. The theoretical model assumes an ideal pinhole projection, but real lenses introduce distortions (typically barrel distortion in wide-angle lenses) that alter the magnification across the field of view. This causes the measured linear FOV to be slightly different from the ideal, directly affecting the calculated experimental AFOV. Other sources of error include the manufacturing tolerance of the focal length ($f$). The close agreement between the horizontal and vertical errors of the NIR system (0.8\% and 0.4\%, respectively) suggests that any residual sensor tilt is negligible.\\

\noindent The experimental characterization successfully validates the angular performance of both optical systems, confirming that they approach their theoretical specifications under far-field conditions. However, it also reveals the presence of small, systematic errors consistent with the effects of lens distortion. To mitigate these effects and achieve the maximum geometric accuracy required for the generation of high-quality orthomosaics, performing a full geometric calibration is recommended. This procedure would allow for the creation of a distortion correction model and the refinement of the intrinsic camera parameters, ensuring greater fidelity in the final data products.\\
%---------------------------------------------------------------

\subsection{Primary Magnification (PMAG) of Systems 6 (NIR) and 7 (VIS)}
\label{sec:pmag}
%---------------------------------------------------------------

\noindent On the other hand, from Equation \ref{eq:pmag}, the primary magnification was obtained for both NIR and VIS systems for each altitude value, as shown in Table \ref{tab:pmag_sys67}.\\

\begin{table}[H]
\centering
\caption{Primary Magnification (\textit{PMAG}) of VIS (Sys.~7) and NIR (Sys.~6) systems at different altitudes.}
\label{tab:pmag_sys67}
\begin{tabular}{|c|c|c|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    \textbf{Altitude [m]} & \textbf{PMAG VIS (Sys.~7)} & \textbf{PMAG NIR (Sys.~6)} \\
    \hline
    50   & $1.62\times10^{-4}$ & $1.53\times10^{-4}$ \\
    1000 & $8.12\times10^{-6}$ & $7.63\times10^{-6}$ \\
    5000 & $1.62\times10^{-6}$ & $1.53\times10^{-6}$ \\
    \hline
\end{tabular}
\end{table}

\noindent For the proposed operational range (50–5000 m), the differences between both systems are $<\,6\,\%$, a consequence of them sharing the same sensor size and having very close focal lengths (8.0 mm – NIR, 8.5 mm – VIS).\\

\subsection{Modulation Transfer Function (MTF) Measurements}
\label{sec:mtf_results}

\noindent Although the experimental setup for MTF characterization was described in the methodology (see Figure~\ref{fig:srf_motaje}), initial tests revealed that the contrast between the light and dark areas was insufficient to reliably apply the ISO 12233 method. According to this standard, the test chart must have a minimum contrast of 4:1 and as uniform an irradiance distribution as possible in both regions, so that the intensity histogram exhibits two sharp peaks (one corresponding to the light areas and another to the dark areas). However, upon analyzing the test chart printed on matte paper—a low-cost reproduction—a continuity of intermediate values and multiple spurious peaks were observed, compromising the accurate extraction of the Edge Spread Function (ESF). This test chart can be purchased from optical instrument suppliers, but its cost is high.\\

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\linewidth]{Figures/C4/MTF_chart_hist.png}
    \caption{Histogram of the ISO 12233 test chart printed on matte paper. Three peaks are visible in the dark region and two very wide peaks in the light region, reflecting a high standard deviation and contrast lower than required by the standard.}
    \label{fig:histogram_iso12233}
\end{figure}

\noindent The origin of this dispersion lies in two main factors. First, the print quality (printed by a local supplier not specializing in charts for laboratory optical tests) of the test chart did not guarantee sufficiently defined edges or a black level that provided enough contrast or precision in the printing of lines. Second, the illumination scheme did not strictly follow the recommendation of using two homogeneous sources (see Figure~\ref{fig:srf_motaje}), which prevented achieving uniform irradiance on the chart and accentuated shadow and reflection areas. Given this, an empirical test pattern was chosen, consisting of an inclined sheet of paper and a dark background, and the region of interest was limited to an area of 104 $\times$ 104 pixels, so that the illumination could be controlled more rigorously.\\

\noindent The test pattern finally used for the MTF determination—shown in Figure~\ref{fig:enfoque}—was previously corrected, first, for dark current as described in Section~\ref{subsub:corriente_oscura_espectrometro}, and second, for random errors by averaging multiple captures (as detailed in the MTF methodology \ref{sec:mtf_char}). Intensity histograms were generated for each system from these corrected images to verify that an acceptable bimodal distribution was now obtained.\\

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/C4/hist_vis_MTF.png}
        \caption{VIS System: histograms of the R, G, and B channels after corrections.}
        \label{fig:histogram_vis}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{\linewidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/C4/hist_nir_MTF.png}
        \caption{NIR System: histogram of its single channel after corrections.}
        \label{fig:histogram_nir}
    \end{subfigure}
    \caption{Distribution of intensity values in corrected images (dark current and random noise) for the VIS and NIR systems, used for MTF extraction.}
    \label{fig:histogram_vis_nir}
\end{figure}

\noindent With these improved conditions, it was possible to proceed with extracting the ESF and, subsequently, the Line Spread Function (LSF) and its Fourier transform to obtain the MTF curves for both systems. This rigor in correction and illumination control ensures that the resulting measurements faithfully reflect the resolution capability of the evaluated optics.\\

%---------------------------------------------------------------
\subsubsection{MTF Curves for VIS and NIR Systems}
\label{subsub:mtf_curves}
%---------------------------------------------------------------
\noindent The MTF curves were obtained with the \texttt{SE\_MTF} plugin in \textit{ImageJ} following the procedure described in section~\ref{sec:mtf_char}. For the VIS system (System 7), the three channels $R$, $G$, and $B$ were processed independently, as well as the average image (\textit{Mono}). For the NIR system (System 6), only a single monochromatic channel is available.

\begin{figure}[H]
\centering
%-----------------------------------------------------------
\begin{subfigure}[b]{0.95\linewidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/mtf_vis.png}
    \caption{VIS System: MTF of the $R$, $G$, $B$, and average (\textit{Mono}) channels.}
    \label{fig:mtf_vis}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.95\linewidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/mtf_nir.png}
    \caption{NIR System: MTF of the monochromatic channel.}
    \label{fig:mtf_nir}
\end{subfigure}
%-----------------------------------------------------------
\caption{MTF curves obtained with \textit{ImageJ}. The plugin configuration is shown in Fig.~\ref{fig:plugin_config1}. This measurement was taken with a working distance of 925 mm.}
\label{fig:mtf_curves}
\end{figure}

\noindent An analysis of the VIS system's MTF curves (Figure~\ref{fig:mtf_vis}) reveals a slightly difference in performance across the spectral channels. The green channel outperforms the blue and red channels, possessing the largest area under the MTF curve (MTFA)—a metric for overall image sharpness. This indicates that the system resolves high spatial frequencies most effectively in the green spectrum, maintaining better contrast, while its performance is weakest for red light. This behavior is a classic symptom of longitudinal chromatic aberration. The system's lens, with its very large f/1.3 aperture, has an extremely shallow depth of field, causing different wavelengths of light to focus at slightly different distances along the optical axis. During the manual focusing process, it is highly likely that the green wavelengths—to which human vision is most sensitive—were brought into optimal focus. Consequently, the blue and, more significantly, the red wavelengths were left slightly out of focus, leading to the reduction in their measured MTF. While the large aperture was chosen to maximize light throughput, it simultaneously exacerbates this chromatic aberration in a broadband system. In contrast, the monochromatic MTF curve for the NIR system (Figure~\ref{fig:mtf_nir}) shows a single curve, with performance comparable to the green channel of the VIS system, indicating good resolution in the near-infrared region.\\

\begin{equation}
\mathrm{MTFA} \;=\; \int_{0}^{f_c} \mathrm{MTF}(f)\,\mathrm{d}f
\label{eq:mtfa_def}
\end{equation}

\noindent The calculated MTFA values for each channel, integrated up to their measured cut-off frequency ($f_c$), are summarized in Table~\ref{tab:mtfa_results}.

\begin{table}[H]
    \centering
    \caption{Area under the MTF curve (MTFA) for each channel.}
    \label{tab:mtfa_results}
    \begin{tabular}{|c|c|c|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    \textbf{System / Channel} & $\mathbf{f_c}$ [lp mm$^{-1}$] & \textbf{MTFA} [lp mm$^{-1}$] \\
    \hline
    VIS -- $R$     & 57.72 & 34.45 \\
    VIS -- $G$     & 79.36 & 49.58 \\
    VIS -- $B$     & 72.15 & 47.25 \\
    VIS -- Mono    & 72.15 & 45.05 \\
    NIR -- Mono    & 79.36 & 46.48 \\
    \hline
    \end{tabular}
\end{table}

\noindent The MTFA results confirm the visual assessment: the green channel exhibits the highest MTFA (49.58 lp mm$^{-1}$), indicating it preserves contrast most effectively. This performance gap is a classic symptom of \textbf{longitudinal chromatic aberration}. The system's f/1.3 lens has an extremely shallow depth of field, causing different wavelengths to focus at slightly different planes. During manual focusing, the green wavelengths were likely optimized, leaving the blue and red channels slightly out of focus and thus reducing their MTF. The NIR system, being monochromatic, is immune to this effect but shows an MTFA (46.48 lp mm$^{-1}$) that is still below the VIS green channel, suggesting the presence of other monochromatic aberrations (e.g., spherical aberration) common in large-aperture designs.\\

\noindent While MTFA describes overall performance, the system's ultimate resolving power is typically defined by the cut-off frequency ($f_{\text{im}}$) where modulation drops to a specific threshold, in this case, 30\%. Table~\ref{tab:cutoff_meas} lists these values.\\


\noindent In contrast, the NIR system (Figure~\ref{fig:mtf_nir}) operates over a much narrower spectral bandwidth of approximately 50 nm, compared to the ~400 nm range of the VIS system. Consequently, while it is not entirely immune to chromatic aberration, the effects are substantially reduced. Its performance, which also falls short of the theoretical limit, is therefore likely limited by a combination of this mild chromatic aberration and other monochromatic aberrations (such as spherical aberration or coma), which are common in large-aperture optical designs, as well as potential limitations from manufacturing and assembly tolerances.\\

\noindent To improve the performance of the VIS system, a practical approach would be to reduce the lens aperture (i.e., use a larger f-number). This would increase the system's depth of field, making the focus less sensitive to wavelength variations and thereby mitigating the effects of chromatic aberration. It is important to clarify a key optical trade-off: a smaller aperture (e.g., f/2.8) actually \textit{lowers} the theoretical diffraction-limited MTF cut-off. However, in an aberration-dominated system like this one, reducing the aperture significantly cuts down on aberrations, often leading to a \textit{better overall real-world MTF}. % An objective with an f/2.8 aperture could represent a promising compromise, potentially yielding a sharper image across all color channels.\\

\noindent The points where the modulation drops to $30\,\%$ provide the cut-off frequency $f_{\text{im}}$. Table~\ref{tab:cutoff_meas} summarizes these values and compares them with the theoretical specifications (Table~\ref{tab:sys_tables}).\\

\begin{table}[H]
\centering
\caption{Cut-off frequency at $30\,\%$ contrast: measurements vs. specification.}
\label{tab:cutoff_meas}
\begin{tabular}{|c|c|c|c|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    \textbf{System / Channel} &
    $\mathbf{f_{\text{im,\,meas}}}$ [lp mm$^{-1}$] &
    $\mathbf{f_{\text{im,\,theo}}}$ [lp mm$^{-1}$] &
    $\mathbf{\Delta\,[\%]}$ \\ \hline
    VIS – $R$     &   57.72 & 85  & $-32.1$ \\ \hline
    VIS – $G$     &   79.36 & 85  & $-6.6$ \\ \hline
    VIS – $B$     &   72.15 & 85  & $-15.1$ \\ \hline
    VIS – Mono      &   72.15 & 85  & $-15.1$ \\ \hline
    NIR – Mono      &   79.36 & 120 & $-33.9$ \\ \hline
\end{tabular}
\end{table}
%---------------------------------------------------------------
\subsubsection{Cut-off Frequency and Ground Resolution Calculation}
\label{subsub:grd_calc}
%---------------------------------------------------------------
\noindent The ground-resolved distance ($\Delta_{\text{ground}}$) was obtained by applying steps 1–3 from section~\ref{subsub:grd_from_mtf}. For each channel, starting from the $f_{\text{im,\,meas}}$ in Table~\ref{tab:cutoff_meas}, the object frequency $f_{\text{obj}}$ is calculated, and subsequently, $\Delta_{\text{ground}}$. The geometric parameters (PMAG and FOV) come from Table~\ref{tab:grd_fov_clean}; the focal lengths are $f_{\text{VIS}} = 8.5\,\mathrm{mm}$ and $f_{\text{NIR}} = 8.0\,\mathrm{mm}$.\\

%--------------------------------------------------------------
\begin{table}[H]
\centering
\caption{Ground-resolved distance $\Delta_{\text{ground}}$ for VIS (Sys.~7) and NIR (Sys.~6) Systems.}
\label{tab:grd_combined}
\begin{tabular}{|c|c|c|c|c|c|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    \textbf{System} & \textbf{Channel} & \multicolumn{3}{c|}{$\mathbf{\Delta_{\text{ground}}\,[\text{cm}]}$} & $\mathbf{\Delta_{\text{ground}}\,[\text{m}]}$ \\ \cline{3-5}
    \rowcolor[HTML]{EFEFEF}
    & & 50 m & 1000 m & 5000 m & 5000 m \\ \hline
    \multirow{4}{*}{\textbf{VIS (Sys.~7)}} & $R$   & 5.35 & 106.68 & 534.72 & 5.35 \\ \cline{2-6}
                                         & $G$   & 3.89 &  77.59 & 388.91 & 3.89 \\ \cline{2-6}
                                         & $B$   & 4.28 &  85.34 & 427.78 & 4.28 \\ \cline{2-6}
                                         & Mono  & 4.28 &  85.34 & 427.78 & 4.28 \\ \hline
    \textbf{NIR (Sys.~6)} & Mono & 4.12 & 82.57 & 411.79 & 4.12 \\ \hline
\end{tabular}
\end{table}
%--------------------------------------------------------------

%---------------------------------------------------------------
\subsubsection{Comparison of Experimental and Theoretical Ground Resolution}
\label{subsub:grd_analysis_summary}

\noindent To validate the system's real-world performance, the experimentally derived Ground-Resolved Distance (GRD) was compared against the theoretical resolution ($\Delta_\text{MTF}$) from the system's design specifications (Table~\ref{tab:grd_fov_clean}). Table~\ref{tab:grd_comparison_mono} consolidates this comparison for the monochromatic channel of each system and quantifies the discrepancy.

\begin{table}[H]
\centering
\caption{Comparison of theoretical vs. experimental ground resolution for monochromatic channels.}
\label{tab:grd_comparison_mono}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{|c|c|c|c|c|}
    \hline
    \rowcolor[HTML]{EFEFEF}
    \textbf{\shortstack{System /\\ Channel}} &
    \textbf{\shortstack{Altitude\\ \mbox{[m]}}} &
    \textbf{\shortstack{Theoretical\\ GRD \mbox{[cm]}}} &
    \textbf{\shortstack{Experimental\\ GRD \mbox{[cm]}}} &
    \textbf{\shortstack{Discrepancy\\ \mbox{[\%]}}} \\ \hline

    \multirow{3}{*}{\shortstack{\textbf{VIS /}\\ \textbf{Mono}}} & 50   & 3.40   & 4.28   & +25.9\%  \\ \cline{2-5}
                                                                 & 1000 & 69.20  & 85.34  & +23.3\%  \\ \cline{2-5}
                                                                 & 5000 & 346.00 & 427.78 & +23.6\%  \\ \hline
    \multirow{3}{*}{\shortstack{\textbf{NIR /}\\ \textbf{Mono}}} & 50   & 1.90   & 4.12   & +116.8\% \\ \cline{2-5}
                                                                 & 1000 & 39.10  & 82.57  & +111.2\% \\ \cline{2-5}
                                                                 & 5000 & 195.30 & 411.79 & +110.8\% \\ \hline
\end{tabular}
\end{table}



\noindent 
The comparison reveals two distinct outcomes. The VIS system's monochromatic channel shows a consistent discrepancy of approximately 23-26\%. This deviation is expected and can be attributed to a combination of real-world factors not accounted for in the ideal theoretical model, such as imperfect focus, the averaging of performance across all three color channels (where red underperforms), residual optical aberrations, and minor uncertainties introduced by noise during the ESF estimation.\\

\noindent In stark contrast, the NIR system exhibits an anomalously large discrepancy of over 100\%. Rather than indicating a catastrophic failure of the lens, the evidence suggests this is primarily due to an unrealistic or erroneous theoretical specification provided by the manufacturer. Several factors support this hypothesis. First, the VIS system's lens (f/1.3) has a significantly larger aperture than the NIR lens (f/2.5), which would theoretically give it a higher diffraction-limited resolution potential. It is therefore counter-intuitive that the NIR lens's theoretical cut-off (120 lp mm$^{-1}$) is rated so much higher than the VIS lens's (85 lp mm$^{-1}$).\\

\noindent Furthermore, since the exact same methodology was used for focusing and measuring both systems, a major procedural error is unlikely. The most compelling evidence is the similarity in the final results: the measured ground resolution for the VIS Mono (4.3 cm) and NIR Mono (4.1 cm) systems are nearly identical. This strongly implies that the actual real-world performance of both lenses is comparable, and the massive discrepancy stems from a flawed theoretical baseline for the NIR system.\\

\noindent Another contributing factor to the final system performance is the MTF of the sensor itself, whose response is convolved with that of the lens. While the sensor's full MTF curve is unknown, its theoretical resolution limit is defined by the Nyquist criterion, $f_{\text{Nyquist}} = 1/(2s)$, where $s$ is the pixel size (2.2~µm). This yields a theoretical cut-off frequency for the sensor of:
\[ f_{\text{Nyquist}} = \frac{1}{2 \cdot (2.2 \times 10^{-3}~\text{mm})} \approx 227~\text{lp mm}^{-1} \]
Although this value is well above the measured system performance, the sensor's MTF will still degrade the signal at lower frequencies, contributing to the overall system response. However, it does not sufficiently explain the 100\%+ discrepancy observed in the NIR system.\\

\noindent 
In summary, the MTF characterization confirms that the VIS system performs within an expected tolerance of its theoretical limits, delivering a practical resolution of 4.3 cm at 50 m altitude. Crucially, the NIR system achieves a very similar and usable resolution of 4.1 cm at 50 m. The significant discrepancy noted in the results is not interpreted as poor performance but rather as evidence that the manufacturer's theoretical specification for the NIR lens is overly optimistic and likely incorrect. This investigation concludes that both systems have comparable and robust real-world resolving capabilities. While a full geometric calibration is still recommended to compensate for distortion, the primary finding is the re-contextualization of the NIR system's performance as being on par with the VIS system, rather than drastically underperforming against an unrealistic benchmark.\\

\subsection{Spectral Response}
\label{sec:spectral_response}

This section presents the experimental results for the VIS (RGB) and NIR systems, from dark-current characterization to the relative spectral response. All results were obtained with the illumination and filtering configurations described previously (see section \ref{sec:spectral_characterization}).

\subsubsection*{Per-sample acquisition scheme (VIS and NIR)}

\noindent
To ensure traceability of the spectral measurements, the acquisition settings used for each filter stack are documented below. Within each system (VIS or NIR), most parameters were kept fixed to avoid confounding factors; only the exposure time was adjusted per sample to maximize signal without saturation. Table~\ref{tab:sr_common_settings} reports the system-wide common settings, while Table~\ref{tab:sr_perfilter_combined} lists the per-filter exposure times.\\

\begin{table}[H]
    \centering
    \caption{Common acquisition settings for VIS (RGB) and NIR (mono) systems.}
    \label{tab:sr_common_settings}
    \begin{tabular}{|l|c|c|}
        \hline
        \rowcolor[HTML]{EFEFEF}\textbf{Parameter} & \textbf{VIS} & \textbf{NIR} \\ \hline
        Gain               & 0.0       & 0.0 \\ \hline
        Gamma              & 1.0       & 1.0 \\ \hline
        Hue                & 0.0       & --  \\ \hline
        Height [px]        & 714       & 600 \\ \hline
        Width  [px]        & 760       & 600 \\ \hline
        OffsetX [px]       & 900       & 1026 \\ \hline
        OffsetY [px]       & 600       & 650 \\ \hline
        BalanceRatio\_Red  & 0.9990234 & -- \\ \hline
        BalanceRatio\_Blue & 0.9990234 & -- \\ \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Exposure time per filter label for VIS and NIR systems.}
    \label{tab:sr_perfilter_combined}
    \begin{tabular}{|c|l|c|}
        \hline
        \rowcolor[HTML]{EFEFEF}\textbf{System} & \textbf{Filter label} & \textbf{ExposureTime [ms]} \\ \hline
        \multirow{8}{*}{VIS} 
            & 1$\ast$Purple$\ast$Red        & 2859.217 \\ \cline{2-3}
            & 2$\ast$3$\ast$Yellow          & 2916.174 \\ \cline{2-3}
            & 3$\ast$4$\ast$Purple          & 11.391 \\ \cline{2-3}
            & 4$\ast$Dark Blue              & 3998.348 \\ \cline{2-3}
            & 4$\ast$Orange                 & 888.522 \\ \cline{2-3}
            & 5$\ast$6$\ast$Dark Green      & 5661.478 \\ \cline{2-3}
            & 5$\ast$Purple                 & 47000.520 \\ \cline{2-3}
            & 5$\ast$Green                  & 2870.609 \\ \hline
        \multirow{3}{*}{NIR} 
            & 4$\ast$Red                    & 29.372 \\ \cline{2-3}
            & 3$\ast$4$\ast$Purple          & 36.715 \\ \cline{2-3}
            & 2$\ast$5                      & 29.372 \\ \hline
    \end{tabular}
\end{table}

\noindent
For parameters not listed in the per-filter tables, the values remained equal to the common settings defined for each system. The exposure time was tuned to prevent clipping while targeting high signal-to-noise under a constant irradiance reference (see Section~\ref{subsub:longitud_onda_central_potencia}). In the VIS camera, the red and blue white-balance coefficients were held at unity (within rounding) to suppress any firmware color scaling during acquisition. \\

\noindent
Consistent with the laboratory methodology described in Section~\ref{subsub:longitud_onda_central_potencia}, the illumination for each filter combination was adjusted so that the optical power incident on the detector equalled the reference irradiance of \(P_{\mathrm{ref}} = 0.42~\mathrm{mW}\), corresponding to the 532\,nm filter. Under these constant-irradiance conditions, the exposure time was the only acquisition parameter modified between measurements, ensuring that no image reached sensor saturation while maintaining linearity of response across all spectral bands. These constraints guaranteed that the measured variations in digital counts arise solely from the spectral sensitivity of the imaging system rather than from changes in illumination power.



\subsubsection*{Dark current and frame averaging}

For each filter configuration an auxiliary series of dark frames was acquired with identical camera settings. All dark frames were averaged to produce a master dark image per system. Figure~\ref{fig:sr_dark_examples} shows an example of dark images for VIS and NIR, and Figure~\ref{fig:sr_dark_hist} shows the corresponding pixel histograms computed over a central region of interest. These histograms are unimodal and narrow, which indicates stable electronics and provides a baseline for dark subtraction.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.48\linewidth}
        \includegraphics[trim = 0 0 0 25, clip, width=\linewidth]{Figures/C4/dc_examplevis.png}
        \caption{VIS example dark current. Filter 1 $\ast$ Purple $\ast$ Red. Time of exposure = 2859.272 ms.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.45\linewidth}
        \includegraphics[trim = 0 0 0 25, clip, width=\linewidth]{Figures/C4/dc_examplenir.png}
        \caption{NIR example dark current. Filter 3 $\ast$ 4 $\ast$ Purple. Time of exposure = 36.715 ms.}
    \end{subfigure}
    \caption{Example of dark images captured for dark-current correction.}
    \label{fig:sr_dark_examples}
\end{figure}

\noindent
From visual inspection of the master dark frames in Fig.~\ref{fig:sr_dark_examples}, the recorded signal remains close to zero across the field for both systems, as expected for acquisitions performed in complete darkness. Nevertheless, isolated pixels and small clusters exhibit nonzero values associated with electronic noise sources such as dark current, read noise, and fixed-pattern components (e.g., hot and warm pixels). Contrary to initial expectations, the VIS system exhibits a slightly higher mean dark signal and broader histogram than the NIR system, suggesting a marginally stronger contribution of dark current or offset noise in the visible channel under identical acquisition conditions. This behavior implies that, although both systems exhibit comparable overall stability, the VIS detector presents a higher baseline dark level.\\

\noindent
To quantify these observations, an exploratory analysis of dark-signal histograms was performed for every acquisition, grouped by filter configuration and exposure time. Although this procedure was carried out for all filter and exposure combinations, Figures~\ref{fig:sr_dark_examples} and~\ref{fig:sr_dark_hist} present only representative examples corresponding to one VIS and one NIR case. These examples illustrate the general behaviour observed across the dataset.\\

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.48\linewidth}
        \includegraphics[trim=0 0 0 20, clip, width=\linewidth]{Figures/C4/dc_hist_vis.png}
        \caption{VIS dark histogram. 1 $\ast$ Pruple $\ast$ Red.}
    \end{subfigure}\hfill
    \begin{subfigure}[b]{0.48\linewidth}
        \includegraphics[width=\linewidth]{Figures/C4/dc_hist_nir.png}
        \caption{NIR dark histogram. 2 $\ast$ 5.}
    \end{subfigure}
    \caption{Pixel-value histograms examples of dark images.}
    \label{fig:sr_dark_hist}
\end{figure}

\noindent
Figure \ref{fig:vis_dark_triptych} and Table \ref{tab:dark_stats_nir} consolidates, for every acquisition and channel, the mean, variance and maximum of the dark-signal distribution.\\

\begin{figure}[H]
    \centering
    % Update the filename below to match where you saved the PNG from Python:
    \includegraphics[width=\linewidth]{Figures/C4/VIS_triptych.png}
    \caption{VIS dark-signal heatmaps by filter (rows) and channel (columns). Panels show, from left to right, the mean, variance, and maximum of the pixel-value distribution for each acquisition.}
    \label{fig:vis_dark_triptych}
\end{figure}

% --- New compact table: NIR only (keep numbers as in your original table) ---
\begin{table}[H]
    \centering
    \caption{NIR dark-signal summary statistics per sample (mono channel).}
    \label{tab:dark_stats_nir}
    \setlength{\tabcolsep}{8pt}
    \renewcommand{\arraystretch}{1.2}
    \begin{tabular}{|l|c|c|c|}
        \hline
        \rowcolor[HTML]{EFEFEF}
        \textbf{Sample} & \textbf{Mean} & \textbf{Variance} & \textbf{Max} \\
        \hline
        \texttt{2$\ast$5}          & 0.0005 & 0.0005 & 1 \\ \hline
        \texttt{3$\ast$4$\ast$Purple}  & 0.0007 & 0.0007 & 1 \\ \hline
        \texttt{4$\ast$Red}       & 0.0008 & 0.0008 & 1 \\ \hline
    \end{tabular}
\end{table}


\noindent
The variance reported in Table~\ref{tab:dark_stats_nir} was computed using all pixels in each dark frame, measuring the quadratic dispersion around the mean \(\bar{x}\) over \(N\) samples:
\begin{equation}
    \sigma^{2} = \frac{1}{N - 1}\sum_{i=1}^{N}\left(x_{i} - \bar{x}\right)^{2}.
\end{equation}

\noindent
The central tendency remains below one count for the VIS system across more than four orders of magnitude in integration time. However, the case corresponding to the longest VIS exposure in the dataset (\(\tau\!=\!47.0\)~s for 5$\ast$Purple) exhibits the highest variance and maximum pixel value among all configurations. This behaviour suggests that, although the average dark signal remains low (\(0.34\)–\(0.75\)~counts), longer integration times lead to a measurable increase in dark-current accumulation and pixel-to-pixel dispersion. The observed growth in variance with exposure time is consistent with thermally generated charge carriers contributing more significantly to the total dark signal as \(\tau\) increases, reinforcing the expected dependence of dark current on integration time and sensor temperature.\\

\noindent
In contrast, the maxima exhibit a clear exposure-time sensitivity and sample dependence. For short exposures (\(\tau\approx 11\)~ms, 3$\ast$4$\ast$Purple), the maxima are only \(2\) counts in all VIS channels. At intermediate exposures near the multi-second regime (\(\tau\approx 2.9\)–\(4.0\)~s; e.g., 1$\ast$Purple$\ast$Red, 2$\ast$3$\ast$Yellow, 4$\ast$Dark$\ast$Blue, 5$\ast$Green) the maxima rise to the \(4\)–\(8\)-count range, reflecting a small population of warm pixels that integrate charge measurably with time. The longest-exposure VIS case (5$\ast$Purple, \(47.0\)~s) shows markedly larger outliers (up to \(43\) counts in \(B\), \(32\) in \(R\), \(28\) in \(G\)), which is consistent with hot-pixel behaviour whose dark signal grows roughly proportionally with \(\tau\). For the NIR system, all acquisitions were performed at short exposures (\(\tau\approx 29\)–\(37\)~ms) and the maxima remain at a single LSB, while the mean and variance are of the same order (\(\sim 7\times10^{-4}\)~counts), which is the signature of rare one-count excursions over a field that otherwise sits at zero.\\

\noindent
Taken together, these results support the following operational picture. Within the exposure range used for spectral characterization, the bulk statistics of the dark signal (mean and variance) are effectively stationary and independent of the filter-dependent integration time; radiometric performance is thus limited by read noise and digitization at the dark baseline. The exposure-time dependence appears primarily in the distribution tails, where a small set of hot or warm pixels accumulate charge linearly with \(\tau\) and inflate the maxima.


\subsubsection*{Dark-corrected images per filter stack}

For each filter or filter combination, 50 illuminated frames were acquired and dark–corrected by subtracting the corresponding master dark. The corrected frames were averaged to form a single image per filter stack. Figure~\ref{fig:sr_mosaic_vis} shows a mosaic of the averaged VIS images organized by filter label, and Figure~\ref{fig:sr_mosaic_nir} shows the NIR mosaic. The mosaics provide a compact view of the spatial uniformity achieved and of the expected signal increase in the passbands.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Figures/C4/vis_spec_res_mosaic.png}
    \caption{VIS system. Mosaic of dark–corrected, averaged images for all filter stacks used (one tile per filter label).}
    \label{fig:sr_mosaic_vis}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{Figures/C4/nir_spec_res_mosaic.png}
    \caption{NIR system. Mosaic of dark-corrected, averaged images for all filter stacks used.}
    \label{fig:sr_mosaic_nir}
\end{figure}


\noindent To validate that no image was overexposed, instead of relying solely on histograms we computed and summarized the pixel-value quantiles for every dark–corrected, averaged image. Figures~\ref{fig:sr_qheatmap_vis} and \ref{fig:sr_qheatmap_nir} display quantile heatmaps (e.g., $q_{10}$, $q_{50}$, $q_{90}$, $q_{99.9}$) per filter. In all cases, the distributions remain well below the upper limit of the dynamic range, and no clipping was observed within the regions of interest used for the response measurement. As expected, narrowband filters yield more concentrated distributions, while wideband stacks exhibit higher upper quantiles and broader spread.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/vis_quartiles_final.png}
    \caption{VIS system. Quantile heatmaps by filter (rows) and representative quantiles (columns, e.g., $q_{10}$, $q_{50}$, $q_{90}$, $q_{99.9}$). Each panel corresponds to a channel (B, G, R), and all panels share a single colorbar to enable direct comparison on a common intensity scale.}
    \label{fig:sr_qheatmap_vis}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/nir_quantiles.png}
    \caption{NIR system. Quantile heatmaps by filter and representative quantiles (e.g., $q_{10}$, $q_{50}$, $q_{90}$, $q_{99.9}$). Panels share the same color scale, showing that none of the conditions approaches the saturation limit, thus confirming the absence of clipping.}
    \label{fig:sr_qheatmap_nir}
\end{figure}

\noindent The quantile analysis confirms that none of the dark–corrected, averaged images approaches saturation for the exposure times used. Since both cameras deliver 8-bit data, the admissible range is $[0,255]$ in pixel value; across all VIS channels and NIR frames the high-end quantile $q_{99.9}$ remains comfortably below this ceiling. In the VIS dataset, the largest observed upper quantiles occur for the expected channel–filter pairings (e.g., blue-weighted stacks in $B$, orange/red stacks in $G$ and $R$), yet even in these cases $q_{99.9}\!\ll\!255$ in pixel value, indicating a clear headroom to the clipping threshold. The NIR results follow the same trend: $q_{99.9}$ lies around $220$–$230$ in pixel value for the tested stacks, again leaving a safe margin within the 8-bit dynamic range.\\

\noindent The channel-wise VIS behavior is physically meaningful. Blue-leaning stacks (e.g., \textit{5~$\times$~Purple}) raise the blue-channel quantiles relative to green and red, whereas orange/red-rich combinations (e.g., \textit{4~$\times$~Orange} or \textit{1~$\times$~Purple~$\times$~Red}) elevate $G$ and especially $R$. Conversely, combinations with very low in-band transmittance for a given channel yield depressed medians and compressed spreads (e.g., \textit{3~$\times$~4~$\times$~Purple} in VIS shows near-zero $q_{50}$ across channels), which reflects low signal rather than clipping. These regimes will demand longer exposures or higher irradiance if one seeks to maximize signal-to-noise while preserving headroom.\\

\noindent In the NIR system, the single-channel quantiles mirror the nominal band placement of the stacks centered near 820–840\,nm: $q_{50}$ values fall in the tens to low hundreds of pixel value, while $q_{90}$–$q_{99.9}$ approach but do not exceed $\sim\!230$ in pixel value. This confirms that the exposure settings chosen for each combination were conservative enough to prevent highlight loss while still capturing the relative differences in throughput among stacks.\\

\noindent Taken together, the quantile heatmaps and line plots provide a compact, exposure-agnostic validation of the acquisition protocol. The joint observation of (i) $q_{99.9}$ consistently below 255 in pixel value, (ii) physically coherent channel ranking per stack, and (iii) distribution widths that follow the intended pass-band design, demonstrates that the dataset is free of saturation artefacts within the regions of interest used for response estimation and is therefore suitable for reliable spectral-response calibration.\\




\subsubsection*{Relative spectral response}
\label{subsub:relative_spectral_response}

\noindent
The relative spectral response was approximated by computing, for each filter stack, the \emph{total} pixel value in the corresponding dark–corrected, averaged image. No spatial cropping was applied (the sum integrates the full frame), and no additional normalization by exposure time or by filter bandwidth was performed. Each scalar was paired with the experimentally measured centre wavelength of its filter stack, yielding a discrete sampling of the response curves for the VIS and NIR systems.\\

\noindent
The VIS camera exhibits the expected three-lobe structure associated with a Bayer–type mosaic \cite{Bayer1976ColorArray,Gunturk2005Demosaicking} (see Figure~\ref{fig:sr_relative_curves_vis}). The blue channel rises in the short–wavelength region and decays toward the green–red bands; the green channel peaks near the middle of the visible spectrum and attains the highest overall response; and the red channel dominates at the long–wavelength end of the VIS range. This envelope agrees with manufacturer–style quantum–efficiency (QE) plots \cite{AlliedVision_Alvium1800U500_DataSheet} and with published databases of measured camera sensitivities, which indicates that our relative characterization is a good shape–level proxy for the sensor’s QE per channel. A salient feature in the VIS data is that the narrowband, interferential filters at 532\,nm and 633\,nm yield lower response levels than adjacent, broader stacks. This behaviour is physically consistent with the band–integrated measurement implied by Eq.~\eqref{eq:spectral_transfer}. In our setup, the illuminant term \(L(\lambda)\) already includes the action of the filter stack—i.e., \(L(\lambda)=L_{0}(\lambda)\,\beta(\lambda)\)—so the recorded signal reflects the integral of \(S(\lambda)\) against a \emph{filtered} spectrum rather than a delta function at the centre wavelength. Wider pass–bands (larger FWHM) pool energy over a broader spectral interval and yield larger total responses, whereas narrow interferential bands (e.g., 532 and 633\,nm) integrate over a much smaller slice and therefore appear lower than adjacent, broader stacks. Mapping each band–integrated measurement to a single centre wavelength thus introduces an attribution uncertainty that increases with pass–band width and with departures of \(\beta(\lambda)\) from an ideal, homogeneous (e.g., Gaussian) shape.\\

\noindent
For a fixed wattage, longer wavelengths carry more photons per unit power; in addition, the exact passband shape of each stack (true width, tails, asymmetries) redistributes those photons relative to the sensor’s sensitivity peaks and flanks. Two stacks with the same total power can therefore yield different totals if one concentrates energy squarely where the channel is most sensitive while the other spreads it over regions where sensitivity is weaker.\\



\noindent It should also be noted that the measured transmission bands are not delta functions and, in several cases, their maxima do not coincide with the nominal center wavelength. Consequently, mapping each measurement to a single reported center introduces attribution uncertainty. A more faithful descriptor for the acquisition chain is the photon-weighted effective center; when this metric is considered, bands labeled as 532\,nm or 633\,nm may in fact lie a few nanometers short of those values in the effective sense relevant to the camera’s response. This observation helps explain local departures from the expected envelope and underscores the importance of characterizing the spectral shape of each band, not only its nominal center.\\

% --- VIS: figure on its own ---
\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/vis_spec_res.png}
    \caption{VIS system. Relative spectral sensitivity for the $R$, $G$, and $B$ channels. Curves are constructed from \emph{total} framewise pixel sums (no spatial cropping and no exposure–time normalization), mapped to the experimentally measured centre wavelength of each filter stack under constant–irradiance conditions. Shaded bands indicate the standard error across repeated acquisitions.}
    \label{fig:sr_relative_curves_vis}
\end{figure}


\noindent
For the NIR system (Figure~\ref{fig:sr_relative_curves_nir}), the single–channel response forms a smooth, unimodal lobe within the 820–840\,nm window sampled by the selected stacks. The three NIR combinations used here are spectrally compact and relatively uniform in bandwidth (FWHM $\approx$ 17–20\,nm) and were chosen to stay within the theoretical pass–band of the sensor (825–875\,nm).\\ 

\noindent
Under these conditions, the measured totals are relatively homogeneous, as expected when the illumination is maintained at constant irradiance and the sampling window does not venture outside the sensor’s high–sensitivity region.\\

\noindent
Small discrepancies between the three points can be attributed to a combination of pass–band width effects, differences in the detailed spectral shape $\beta(\lambda)$ of each stack, spatial non–uniformities in the illumination after filtering, or residual non–linearities in the acquisition chain. \\

\noindent
A compact measure of relative dispersion across the three NIR responses is the coefficient of variation $\mathrm{CV}=\sigma/\mu$, computed from the total sums $\{3.25\times10^{7},\,2.86\times10^{7},\,3.18\times10^{7}\}$: we obtain $\mu\!=\!3.095\times10^{7}$, $\sigma\!=\!1.71\times10^{6}$, hence $\mathrm{CV}\!\approx\!5.5\%$ and a relative variance $\sigma^{2}/\mu^{2}\!\approx\!3.0\times10^{-3}$. These figures corroborate that the NIR measurements are mutually consistent within a few percent under the present protocol.\\

% --- NIR: figure on its own ---
\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{Figures/C4/nir_spec_res.png}
    \caption{NIR system (mono). Relative spectral sensitivity estimated from \emph{total} framewise pixel sums (no spatial cropping and no exposure–time normalization), referenced to the measured centre wavelength of each stack under constant irradiance. The response exhibits a single lobe centred over the 820–840\,nm window. Shaded band: standard error across repeats.}
    \label{fig:sr_relative_curves_nir}
\end{figure}


\noindent
The foregoing considerations highlight the principal limitation of the current approach: because each point represents a band-integrated measurement, the uncertainty with which it can be ascribed to its centre wavelength scales with the pass-band width and with shape heterogeneity across stacks. To reduce this attribution uncertainty and recover curves that more closely reflect the underlying, pointwise spectral sensitivity $S(\lambda)$, future measurements should employ stacks with homogeneous, narrow pass-bands—ideally quasi-Gaussian and as close as practical to each other in FWHM—or, preferably, a monochromator to realise $\beta(\lambda)$ as a tunable, $\sim$1-5\,nm band. A finer sampling of the VIS and NIR ranges with such narrowband stimuli would allow us to validate whether local depressions (e.g., near 532\,nm and 633\,nm) are genuine features of $S(\lambda)$ or artefacts of bandwidth pooling and spectral–shape variability.\\

\noindent
A full spectroradiometric characterisation remains outstanding: beyond the relative, single-irradiance protocol used here, the camera chain must be exercised under traceable absolute flux, swept irradiance levels, and verified linearity to lock the response onto an absolute scale. Doing so—via neutral-density attenuation, monochromator-based excitation, or equivalently well-behaved narrowband stacks—would confirm that the inferred spectral shape ($S(\lambda)$) stays invariant with input level and would convert the present band-integrated aggregates into calibrated, intensity-independent sensitivities.\\

\noindent
Within those caveats, the measured VIS and NIR responses behave as expected: the Bayer camera delivers the classic three-lobe structure, the mono NIR channel peaks smoothly within the designed band, and the acquisition protocol kept saturation and repeatability in check. These results validate the relative method as a sound proxy for shape analysis while setting the stage for the absolute radiometric campaign needed to close the loop with mission-level requirements.\\


\section{Comparison with Requirements}
\label{sec:synthesis_requirements}

\noindent
Overall, the VIS/NIR system behaves in line with physical expectations for a Bayer–mosaic RGB camera and a mono–NIR sensor: the visible channels show the canonical three–lobe envelope and the NIR channel exhibits a smooth, single lobe within the targeted 820–840\,nm window. From a practical standpoint, the protocol avoided saturation, maintained constant irradiance at the detector, and produced internally consistent trends across filter stacks, which meets the baseline requirement of delivering a repeatable, relative spectral characterization suitable for downstream normalization and algorithm development.\\

\noindent
It should be noted, however, that this study did not include a performance comparison against typical Earth–surface reflectance spectra. Such a comparison matters for remote–sensing usage where the scene spectrum is the product of solar illumination and surface reflectance. Reference libraries (e.g., USGS/ECOSTRESS/ASTER) report well–known spectral signatures: healthy vegetation with low reflectance in the visible, a sharp rise across the red–edge near 700–750\,nm, and high reflectance in the NIR; soils with gradually increasing reflectance from blue to NIR and diagnostic features in the short-wave infrared; water with very low reflectance in the visible/NIR except for glint; and bright snow with high visible reflectance that decreases with grain size and impurity content \cite{USGS_SpecLib_v7,ASTER_Baldridge2009,Seager2005RedEdge,Knipling1970,USGS_Landsat_SR}. Incorporating these spectra in a requirement–to–result trace would allow quantifying expected bandwise signal levels for canonical targets (vegetation, bare soil, water, snow/asphalt), thereby turning the relative sensitivity curves into scene–level detectability predictions.\\

\noindent
Likewise, the present work did not perform an absolute radiometric characterization. Establishing radiometric gain and linearity as a function of irradiance is essential to predict behavior under varying illumination: from near–noon conditions, where downwelling solar irradiance is close to the AM1.5G reference and reflected radiance is maximal for Lambertian bright targets, to low–sun or twilight regimes with significantly reduced flux \cite{NREL_AM15,Myers2011_ASTMG173}. Here, AM1.5 refers to the ASTM~G-173 reference spectra at air mass 1.5 (solar zenith $\approx 48.2^{\circ}$), with two variants: AM1.5D (direct-normal) and AM1.5G (global on a $37^{\circ}$ tilted plane), with integrated irradiances of $\approx\,900\,\mathrm{W\,m^{-2}}$ and $1000\,\mathrm{W\,m^{-2}}$, respectively. Such a calibration would enable answering requirement–level questions like: What is the minimum scene irradiance at which the sensor maintains a target SNR for vegetation or water? How much exposure time or analog gain is admissible before SNR gains are offset by read noise, dark current growth, or motion blur? And, under which conditions does the tone mapping (if any) risk compressing contrast for low–reflectance targets?\\

\noindent
These radiometric considerations directly inform system design trade–offs. Sensor choice (pixel size, full–well capacity, read noise) must be balanced against geometric requirements (ground sampling distance and field of view). Larger pixels generally increase spectral/radiometric sensitivity but, for a fixed optical format, reduce spatial resolution; conversely, smaller pixels improve spatial detail but demand more photons per pixel to reach a given SNR. For any given mission profile (solar zenith angles, target types, flight altitude), defining requirement envelopes that couple spectral/radiometric sensitivity with geometric constraints would clarify whether the current sensor/optics are adequate or whether a different pixel pitch, lens $F/\#$, or exposure strategy is required.\\

